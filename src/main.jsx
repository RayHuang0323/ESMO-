import React from "react";
import ReactDOM from "react-dom/client";
import EsportsGame from "./EsportsGame.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EsportsGame />
  </React.StrictMode>
);
