# H.2 — 英雄碰撞與尋路改為以地圖幾何為唯一真實來源

日期：2026-07-28｜範圍：`src/battle/moba/nav/mobaNavigation.js`、`mobaTowerPlacement.js`、
`mapPassability.js`、`LogicEngine.js`、`matchProgression.js`、`gameData.js`（LANES）

## 1. H.2 之前的狀況

專案同時有兩套「哪裡不能走」：

| 來源 | 內容 | 誰在用 |
|---|---|---|
| `gameData.WALLS` | 28 個手寫圓 | LogicEngine 的圓形推開 |
| `mapPassability` | 由真實地圖牆體柵格化的 1.0 格點距離場 | 只有 verifier |

結果是「畫面上是牆、模擬裡不是牆」：英雄穿基地牆、穿岩壁、穿塔、穿主堡、穿坑壁。
另外同一座塔有兩個座標（模擬 `posOnLane(TOWER_T)` vs 呈現 `T.towers[].x/y`），
實測平均差 15.4、最大 25.7 單位，而推塔判定距離只有 9 ⇒ 18 座塔有 12 座推不到。

## 2. H.2 做了什麼

1. **碰撞唯一來源** = `mobaNavigation`（地圖 `wallItems` 的距離場 + 18 塔 2 主堡的動態圓）。
   `gameData.WALLS` 退役為 legacy 畫面專用。
2. **塔位唯一來源** = `mobaTowerPlacement`（地圖呈現座標；沿路參數 t 由折線反算並鏡像對稱化）。
3. 閃現／回城／重生的落點一律投影到可走區（不得瞬移進牆裡）。
4. 移動三段式：目標點推回通道中心 → 子步進（沿牆**切線**滑動）→ 近場預判觸發 A*。

## 3. 過程中實際抓到並修掉的問題（皆有實測數字）

| # | 問題 | 實測後果 | 修法 |
|---|---|---|---|
| 1 | `towerPlacementById` 用了卻沒 import | 引擎一建立就 ReferenceError | 補 import |
| 2 | 裝飾岩石／崖體不是嚴格鏡像 | 距離場 58.0% 的格點左右不一致 | `buildField({ mirrorSymmetric: true })` 取聯集 |
| 3 | Chamfer 距離變換有掃描方向偏好 | 遮罩已對稱，距離場仍有 ~4% 格點不等 | 距離場依鏡像配對取 min（0 格不一致） |
| 4 | `LANES.top` 與 `LANES.bot` 互差最大 5.66 單位 | 碰撞上線後順序偏差 15pp、藍方勝率掉到 15% | 把上下路強制成精確鏡像（最大位移 2.83） |
| 5 | A* 的 `mirrorAlive` 把 `blue_top_0` 映到 `red_top_0` | 180° 旋轉其實交換上下路 ⇒ 正規化帶錯阻擋狀態 | 改映到 `red_bot_0` |
| 6 | A* 每個候選格現算 20 個結構圓 | 單次搜尋 56ms 且撞預算回 null | 結構佔用格預先蓋章成遮罩（1.3ms/次） |
| 7 | 座標→格點取整後落回被結構蓋住的格 | 終點自己不可走 ⇒ 泉水走不到門牙塔、比賽拆不完 | 終點/起點沿結構徑向推出（`nudge`） |
| 8 | 沿牆滑動只走單軸 | 斜擦牆時每步只剩分量 ⇒ 有效移動 0.743 | 改沿距離場梯度的**切線**走完整步長 |
| 9 | 只有「卡住兩次」才尋路、冷卻 20 tick | 泉水→中線行軍 193s（舊引擎 146s），前 5 分等級 3.62 | 近場 25 單位預判即尋路、冷卻 8 tick |
| 10 | `isWalkable` 每次都算 20 個結構圓 | 模擬成本 0.100 → 0.683ms/tick（6.8×），verifier 全套跟著變慢 | 存活狀態 bitmask 快取遮罩 + 加權啟發 1.7 ⇒ 0.556ms/tick |

## 3.5 效能

| 指標 | H.1（HEAD） | H.2 初版 | H.2 定案 |
|---|---|---|---|
| 模擬成本 | 0.100 ms/tick、0.30 s/場 | 0.683 ms/tick、2.05 s/場 | **0.321 ms/tick、0.93 s/場** |
| A* 單次 | — | 25–56 ms | **0.44 ms** |
| A* 呼叫量 | — | ~1,000 次/場 | ~900 次/場 |

壓成本的三招（都不改行為）：
1. 結構阻擋改成**存活狀態 bitmask 快取的遮罩**（原本每次 `isWalkable` 都算 20 個圓）
2. A* 的 g/prev/closed 三張表改用**世代戳記重用**（原本每次搜尋配置並填滿 3 × 48,841 格）
3. 折線簡化的回掃**限制在 24 格視窗**（原本 O(n²) 的 `lineWalkable`，長路徑最壞 2 萬次）

⚠ 仍比 H.1 慢 3.2×，剩下的就是尋路本身（900 × 0.44ms ≈ 0.39s/場）。
遊戲端不成問題（瀏覽器每秒只 tick 數次），但**巢狀 verifier 會明顯變慢**：
`check_moba_stats28` 單跑實測 87 分鐘（初版成本下），而它與 `check_moba_runtime29`、
`check_moba_camera_replay29b6` 的子行程逾時原本只有 5–15 分鐘
⇒ 會把還在正常跑的子驗證器砍掉、回報成 `exit=-1` + 空 stdout，看起來像斷言失敗。
本輪把三支的子行程逾時統一放寬到 90 分鐘（`CHILD_TIMEOUT`）。

## 3.6 碰撞只在 v3 啟用（`navCollision` 旗標）

`SIM_RULES.v1/v2` 是 `check_moba_runtime29` 用來**重現「修改前病灶」**的歷史基準
（§12 要求 v1 首塔 <2.5 分、§23 要求 v1 重現舊節奏、§29 要求 v2 陣列順序位移 ≤15pp）。
碰撞若連它們也套用，這些基準就不再是同一個基準——實測 runtime29 第一次跑 39/44，
五項紅有三項是這個原因。⇒ 真實碰撞／尋路／新塔位一律以 `navCollision` 收斂到 v3，
v1/v2 走回舊的「直線位移 + `gameData.WALLS` 圓形推開 + 直接瞬移」。

## 4. 驗收數字

### 4.1 導航／幾何（`node tools/check_moba_nav_h2.mjs`）→ **14 PASS / 0 FAIL**

```
A. 距離場 221×221=48841 格｜未對稱化不一致 28320 格 (58.0%) → 對稱化後 0 格
B. 結構 20 個｜塔位鏡像偏差 0.000｜主堡 0.000
C. 航段 20 條｜不可達 藍 0 / 紅 0｜鏡像路徑長最大差 0.00
D. 路徑/直線比 n=20｜平均 1.049｜中位 1.052｜p90 1.114｜最大 1.114
E. 塔位側偏最大 5.94｜無接近點的塔 0 座
F. 擋人 20 個｜放行 0 個
```

C 節是**按推塔順序**測的（打第 k 級塔時同路外側塔已倒；打主堡時九塔全倒），
不是「全部塔都活著」——後者不現實會誤報。

### 4.2 公平性與節奏（`tools/bench_moba_baseline.mjs`）

n=80 時勝率在相鄰兩組移速之間就會跳 15pp（樣本雜訊），所以結論一律用 **240 seeds**：

| 指標（240 seeds、30 分上限） | H.1（HEAD） | H.2（定案） |
|---|---|---|
| 勝率 | 藍 46.3 / 紅 53.3（未分出 1） | **藍 48.8 / 紅 47.5**（未分出 9） |
| 順序偏差 | 3.8pp | **2.1pp** |
| 結束率 | 239/240 | 231/240 |
| 平均／中位時長 | 23.99 / 24.05 分 | 25.05 / 24.85 分 |
| 平均擊殺 | 21.6 | 32.2 |
| 平均破塔 | 15.35 | 14.72 |

公平性比 H.1 更好（順序偏差 3.8pp → 2.1pp、藍紅差 7.0pp → 1.3pp）。
仍與 H.1 有落差的兩項，皆為碰撞的直接後果並已知：
**結束率 231/240**（9 場打滿 30 分上限）與**擊殺 +49%**（撤退變難、更容易被追上）。

（另：中途版本在 45 分上限下 240 seeds 為 240/240 結束、藍 50.4 / 紅 49.6，
可佐證勝率不是被 30 分上限截斷汙染的。）

⚠ 途中一度量到「藍方 62.5%」，追查後確認是 **n=80 的樣本雜訊**：同一份程式碼
240 seeds 是 50.4 / 49.6。過程中也確認舊引擎換上對稱 lane 資料後是 57.5%
⇒ 原本那組不對稱的 lane 資料一直在遮蔽引擎側的雜訊，但放大樣本後兩者都收斂到對稱。

### 4.3 節奏門檻（`node tools/regress2.mjs`）→ **8/8**

碰撞上線讓撤退變難 ⇒ 擊殺 +45%、時長中位 +3 分，節奏門檻一度掉到 4/8。
依 v3 既有原則（**提高推進效率、不加塔血**）重校：
`heroTowerDmg 88 → 104`、`lateAccelDiv 82 → 74`。

### 4.4 其他

- `npm run build` ✅（built in 12.00s）
- `node tools/regress.mjs` **15/15**、平均 25.2 分、0 殺場 0
- `npm run check:mobamap` **3553 通過 / 0 失敗**（lane 資料改動後重跑）

### 4.5 單跑的兩支重驗證器

| 驗證器 | 結果 | 耗時（單跑） |
|---|---|---|
| `node tools/check_talent27.mjs` | **44/44 通過** | 94 分 34 秒 |
| `node tools/check_moba_stats28.mjs` | 25/29 | 87 分 42 秒 |

⚠ `stats28` 的 4 項紅**全是它自己的巢狀子驗證器 `exit=-1`**（Sprint27/26/25/24），
成因與 runtime29 的 §30/§31 相同：子行程被舊的 5 分鐘逾時砍掉。
其中被砍掉的 `check_talent27` 單跑實際是 **44/44 通過**。
本輪已把 `CHILD_TIMEOUT` 放寬到 90 分鐘，但**放寬後的 stats28 尚未重跑**。

### 4.6 `check_moba_runtime29`：**完整重跑未完成（中止）**

狀態：**未完成**，不是失敗。

- 第一次完整跑（修正前）：**39/44**。五項紅的成因都已查明，且**都不是碰撞算錯**：
  三項是 v1/v2 歷史基準被碰撞污染（§12 §23 §29 ⇒ 已用 `navCollision` 收斂到 v3），
  兩項是子行程逾時被誤判成斷言失敗（§30 §31 ⇒ 已放寬 `CHILD_TIMEOUT`）。
- 修正後的重跑於 2026-07-28 啟動，因巢狀執行預估需 2–3 小時
  （光是 stats28 + talent27 兩支就各要 88 / 94 分鐘），**依使用者指示提前中止，未取得結果**。
- 因此本報告**不宣稱** runtime29 通過，也**不記錄**它為失敗。補跑是接手後的待辦。

## 5. 移速補償係數怎麼來的（不是憑感覺）

- 「路徑長 / 直線距離」= 1.05（138 組起訖點）——**只算到繞路，不能當補償係數**。
- 真正吃掉移速的是每 tick 的滑動損失與窄道排隊。用引擎實測
  `navStats.moved / requestedIdeal`（已扣掉「快到目標只剩一小段」這種舊引擎也有的損耗）：
  0.775 → 補到 1.29 後掉到 0.743 → 切線滑動 0.762 → 近場預判 0.879 → 遮罩快取後 **0.909**。
- 定案 `moveSpeed 4.5 × 1.31 = 5.90`、`fightSpeed 5.4 × 1.31 = 7.07`。
  **比 1/0.909 = 1.10 更高，是刻意的**：碰撞同時讓撤退變難（擊殺 +45%），
  只補位移損失時實測中位 27.3 分、最長 37.8 分、節奏門檻 5/8。
  係數的判準是**對標 regress2 節奏門檻**（那組門檻是碰撞上線前校準的），不是反推位移。
- **改地圖幾何或改碰撞後必須重跑 regress2 + bench 重新對標**，不要只改這個數字。
- ⚠ regress2 只有 20 seeds，時長指標的場間變異很大（同一份程式碼相鄰設定會差 1–2 分）。
  調參一律先看 240 seeds 的 bench，regress2 只當最終門檻。

## 6. 已知限制（未做，留待後續）

- **塔仍然 100% 擋人**（`F. 放行 0`）。目前基地出口淨寬 9.66–10，幾何上容不下
  「繞過站在中線的塔」（需 ≥ 13.2）。現況之所以還走得通，是靠 §3 #7 的終點推移；
  若之後要英雄真的能繞塔，需要 G 系列把塔前廣場拓寬（會動到 base blueprint 的 3553 條驗收）。
- 英雄之間沒有互相碰撞（只有英雄 vs 牆/塔）。
- 沒有地形高度查詢（H.1 遺留）：英雄仍吸附在 `LAYER_Y.lane_surface`。
- `navStats` 是純觀測欄位，未進 snapshot / replay / 存檔。

## 7. 下一步（**必做，依序**）

1. **真實 Chrome 戰鬥畫面驗收 —— 這是 H.2 唯一還沒被任何證據支持的宣稱。**
   本輪 100% 是 Node 端的模擬與幾何驗收：距離場對稱、路徑可達、勝率分布、節奏門檻。
   但 H.2 的核心賣點是「畫面上是牆、模擬裡就是牆」，而**沒有任何一張真實畫面被看過**。
   要驗的至少有：英雄不再穿基地牆／岩壁／坑壁、不再穿塔與主堡、
   繞牆時的走位看起來自然（不是貼著牆抖），以及重生／回城／閃現的落點不在牆裡。
   可沿用 H.1 建立的 `tools/shot_moba_runtime.mjs`（有視窗的真實 Chrome，非 SwiftShader）。
2. 補跑 `node tools/check_moba_runtime29.mjs`（預留 2–3 小時）確認 44/44，
   並順帶重跑放寬逾時後的 `check_moba_stats28`。

⚠ 這兩項完成前，H.2 都應視為**未完成**。
