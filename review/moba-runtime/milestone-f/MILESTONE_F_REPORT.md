# Milestone F Report — MOBA 戰鬥節奏與團戰收益轉化

日期：2026-07-30

狀態：**本機實作與 Node 驗證完成；未 push、未部署，等待 Ray 驗收。**

rollback tag：`milestone-f-baseline` → `2500ae2`（＝ Milestone E 完成並已部署的狀態）

## 1. 先量再改：E baseline 的數字

動任何權重之前先建立儀器 `tools/measure_moba_pacing.mjs`（只讀引擎狀態、不改行為，
可在任意 commit 上跑）。40 seeds、正／反序各一輪，量到的問題：

| 指標 | E baseline（正／反序） | 說明 |
|---|---|---|
| 團戰窗／場 | 20.78／23.40 | 太多 |
| **零碎碰撞率** | **0.49／0.51** | 半數「團戰」短於 3 秒且零陣亡 |
| 團戰中位長度 | 2.0 秒 | 根本沒形成 |
| 決勝團戰／場 | 2.78／2.48 | 有勝負的極少 |
| **打贏後的無目的遊走率** | **0.38／0.37** | 打贏一波後 25 秒內，勝方近四成時間只是在對線／游走 |
| 團戰後硬收益轉化 | 0.23／0.28 | S28 技術債 2 的量化 |

另一個關鍵觀察（seed 1000 儀器化）：**42 個擊殺裡有 31 個發生在引擎根本沒有
認定團戰的時候**。引擎的 `hot` 抓到的多半是「兩邊各兩人對峙但沒人死」，
真正的收益機會是抓單與以多打少 —— 所以「轉化」不能只綁團戰窗。

## 2. 三個根因與修法

### F1 團戰窗遲滯（節奏）

根因不是熱點難成立，而是**接觸一斷就立刻解散**：每次解散都送出 DISENGAGE ＋
13 秒重接戰冷卻，同一場遭遇被切成好幾段擦撞。

- 成立條件**完全不放寬**（一樣每側 2 人 ＋ 實際接觸 6 單位）
- 成立後給 `fightHoldT = 3` 秒遲滯：接觸暫斷仍算同一場
- `fightMaxDur = 20`：團戰不會僵持一整場，超時強制結算再重開
- `fightHoldAfterT = 240`：**對線期不套遲滯**（早期本來就該是短交鋒；
  實測不加這個閘門會把 5 分鐘均等級壓到 2.4，低於 regress2 門檻 2.5）

### F2 主動權窗（收益轉化）

打贏一波之後開一個 22 秒的窗，把戰果導向**具體目標**：

1. 附近有存活的巴龍且勝方 ≥3 人 → 巴龍
2. 否則附近有存活的龍 → 龍
3. 否則 → 推離戰場最近的敵方建築

實作刻意「接既有路徑」而不是另寫一套：龍／巴龍直接打開既有的目標窗
（距離、承諾上限、打野輔助必去、knob 單調性全部沿用），攻城才新增一個移動分支。
殘血的人不跟進（走既有回城邏輯）——贏了團戰不該接著送人頭。

觸發點有兩個：團戰結算（`_resolveFightV3`）與**擊殺事件**（`_maybeInitiativeV3`）。
只綁團戰窗時實測轉化率量不動（0.23 → 0.24），因為八成的擊殺不在團戰窗內。

### F3 群體拆塔分級與追擊約束

- `heroTowerSoloK = 0.30` 的原意是「**孤軍**拆不動」，但實測打贏一波後三四個人
  站在塔下仍吃 0.30 ⇒ 22 秒的窗根本推不掉一座塔。新增
  `heroTowerGroupMin = 3 / heroTowerGroupK = 0.62`：成群集火給較高效率，
  仍明顯低於有兵線的 1.0。**沒有改塔血、沒有改 `heroTowerDmg`。**
- PURSUE 不再追進敵塔射程（除非我方在該塔區有人數優勢）。

## 3. 結果（40 seeds，正／反序）

| 指標 | E baseline | Milestone F | 判定 |
|---|---|---|---|
| 團戰窗／場 | 20.78／23.40 | **13.53／14.35** | ✅ 變少 |
| 零碎碰撞率 | 0.49／0.51 | **0／0** | ✅ 消除 |
| 團戰中位長度 | 2.0／2.0 秒 | **7.25／6.38 秒** | ✅ 真的形成了 |
| 決勝團戰／場 | 2.78／2.48 | **3.58／4.45** | ✅ 更常分出勝負 |
| **無目的遊走率** | 0.38／0.37 | **0.20／0.18** | ✅ 減半 |
| 團戰後硬收益轉化 | 0.23／0.28 | **0.30／0.30** | ✅ 提升 |
| **機會事件硬收益率** | 0.36／0.35 | 0.37／0.34 | ⚠ **持平，見下** |
| 平均時長 | 25.26／26.23 分 | 25.12／25.04 分 | 穩定 |
| 平均擊殺 | 34.0／36.7 | 36.7／36.0 | 穩定 |
| 平均破塔 | 17.08／16.90 | 17.30／16.90 | 穩定 |

### ⚠ 誠實揭露：有一項沒有改善

「機會事件硬收益率」（每一次**打贏一波**之後 25 秒內是否拿到塔／龍／巴龍／野區）
**幾乎沒有變**（0.36 → 0.37）。改善的是「有沒有在做事」（遊走率減半）與
「團戰型」轉化（0.23 → 0.30），不是每一次優勢都變成硬收益。

原因是分母裡有相當比例的機會本來就換不到硬收益：目標在冷卻、最近的塔太遠、
或勝方選擇回城補給（那是正當決策，本報告的另一個指標把回城算成有目的）。
**我沒有為了讓這個數字好看而放寬定義或調參**。若 Ray 認為這一項才是驗收重點，
需要的是更長的窗或更強的推進效率，那會再次動到時長與公平性分布，屬於另一輪校準。

## 4. 公平性與節奏門檻（重新校準後）

| 驗證 | 結果 |
|---|---|
| `check_moba_pacing29b1`（**官方公平性**） | **25/25 PASS**；§25：40 seeds 正序藍 21/40、反序 26/40、**位移 13pp ≤ 15**，兩者皆在 30–70% |
| `regress2` 節奏門檻 | **8/8 PASS**（平均 24.6 分、中位 25.1、最長 30.3 ≤ 32、5 分塔 0.00、5 分均等級最低 2.6 ≥ 2.5） |
| `regress` | 結束率 **14/15**（baseline 15/15）、平均 24.7 分、擊殺 33.9、撤退鎖死 0 |
| `check_moba_milestone_f`（新增） | **30/30 PASS** |
| `check_moba_milestone_e` | 49/49 PASS（E 的名單資料流未受影響） |
| `check_moba_milestone_d_fix3`／`d_fix2`／`d`／`c_fix` | 全部 PASS |
| production build | 2596 modules、exit 0 |

**兩項要 Ray 知道的分布變化**：

1. `regress` 結束率 15/15 → **14/15**（一場超過它的 30 分鐘取樣上限）。
   regress 本身 exit 0（非硬門檻），而硬門檻 `regress2` 的「最長 ≤32 分」是 30.3 分通過。
2. 正／反序位移從 0pp 變成 **13pp**（官方儀器）。仍在 ≤15pp 門檻內、兩側都在
   30–70%，但**餘裕比 D-fix2 當時小**。這是混沌放大而非系統性偏袒
   ——F 的機制經行為對稱測試（verifier §25：雙方互換戰況 ⇒ 結果完全鏡射）。

## 5. 修改檔案

- `src/LogicEngine.js`（團戰窗遲滯／`_resolveFightV3`／`_openInitiativeV3`／
  `_maybeInitiativeV3`／`_initiativeSiegeV3`／攻城移動分支／群體拆塔分級／PURSUE 約束）
- `src/battle/moba/matchProgression.js`（v3 新 knobs；v1／v2 一個字都沒改）
- `tools/measure_moba_pacing.mjs`（新增，量測儀器）
- `tools/check_moba_milestone_f.mjs`（新增，30 條安全網）

**未改**：地圖幾何、碰撞／導航、`MobaReplay.v1`、`BattleResult.v2`、
Milestone E 的名單資料流、視覺與呈現層（verifier §5 有靜態斷言守住）。

## 6. 正式 GameView 瀏覽器驗收（2026-07-30 補做）

工具：`tools/shot_milestone_f.mjs`（真實 Chrome + CDP，走正式流程進 GameView，
4× 看完整場，每秒取樣一次）。量的是 Node 證不了的那一半。

### 桌機 1600×1000 — 完整一場（跑到 `over`，313 個取樣點）

| 觀察項 | 結果 | 判讀 |
|---|---|---|
| FPS | 中位 **61**、p10 58、最低 46（313 秒中只有 7 秒 <50） | 可接受 |
| 群聚度（12 單位內人數上限） | 最大 8、**平均 2.59**、≥6 人只有 **4 秒** | **沒有過度群聚** |
| 抖動（每秒位移方向反轉） | 354 次／約 2800 hero-秒 ≈ 0.13 | 偏低 |
| 卡位 | 103／1491 hero-秒（6.9%），狀態分布 **回防 47、對線 31、打野 18、避塔 6、團戰 1** | **不是卡位**：全是「站著才對」的狀態 |
| 英雄離開畫面 | 最多 8（導播鏡頭跟團戰時的正常現象） | — |
| runtime 警告 | 0 | — |

**行為分布（hero-tick 佔比）**——這一項直接回答「團戰後有沒有轉向做事」：

```
對線 40.9% ｜ 團戰! 16.7% ｜ 打野 7.8% ｜ 回防 6.7% ｜ 圍攻 5.4%
回城中 4.2% ｜ 避塔 4.0% ｜ 撤退 3.8% ｜ 抓人 2.7% ｜ 回城 1.7%
脫戰 1.4% ｜ 接戰 1.3% ｜ 追擊 0.9% ｜ 圍攻主堡 0.6% ｜ 拉扯 0.3% ｜ 支援 0.1%
```

圍攻＋圍攻主堡 6.0%、回城＋回城中 5.9%、追擊 0.9% —— 團戰之後確實轉向推塔／
回城／追擊，不是原地遊走。畫面證據：`F04-desktop-teamfight-8m49.png`（巴龍團）與
`FC-desktop-16min.png`（巴龍 772/3000，播報依序出現「一起壓 下路 塔」→
「反攻成立，上」→「圍了！全上」→ 懲戒 → 「中路 進塔，掩護我」→ 擊殺 → 「團戰！集火」）。

### 手機 390×844

| 觀察項 | 結果 |
|---|---|
| FPS | 中位 **61**、最低 60 |
| 水平溢出 | 無 |
| 小地圖 vs 十人面板重疊 | **無** |
| 群聚度 | 最大 7、平均 2.67 |
| 隊伍面板展開 | 五列可讀、不擋 HUD（`FC-mobile390-8min-panel.png`） |

### ⚠ 兩個**既有**視覺問題（非 F 造成，本輪只記錄不修）

1. **巴龍模型在坑區呈現為一大片淺色平面**（`FC-desktop-16min.png` 畫面中央）。
   F 沒有動任何呈現層，但 F 讓雙方更常在巴龍坑集結 ⇒ 這個問題更常被看到。
2. **手機的擊殺橫幅過大**：`Double Kill! R2`／`Dragon 被紅方擊殺` 會蓋到
   倍率與畫質按鈕（`FC-mobile390-14min.png`）。同樣是既有呈現層行為，
   但 F 讓決勝團戰變多 ⇒ 橫幅出現頻率上升。

## 7. 未驗證（不宣稱通過）

1. **Android 真機**（FPS／熱降頻／觸控／safe area）——D-fix3 的清單仍全部有效。
   桌面 viewport 的 390×844 不等於真機。
2. **長時間人眼觀感**：本輪是工具取樣＋定點截圖，沒有連續人眼觀看一整場。
   團戰的「好不好看」最終仍需 Ray 自己看一場。
3. `check_moba_runtime29` 的狀態見 §8。

## 8. `check_moba_runtime29` 分段驗證結果：**39/44 → 收尾校準後 40/44**

完整跑完（約 100 分鐘，巢狀 `stats28`）。五條紅燈，逐條歸因：

| 項目 | 內容 | 性質 |
|---|---|---|
| §29 v2 陣列順序 | v2 藍勝 正序 55%／反序 35%、**位移 20pp** | **既有**。Milestone D 報告已記載同一條、同一組數字（22/40 對 14/40、20pp），並以 `milestone-d-baseline` 重現過。F 完全沒有動 v1／v2（本階段 verifier §21／§22 有專門斷言） |
| §30 `stats28` 29/29 | exit=1 | **既有**：Replay 容量紅燈往上串 |
| §31 `talent27` 44/44 | exit=1 | **既有**：同一條串紅 |
| §32 `experience26` 35/35 | exit=1 | **既有**：§17 replay size（baseline 2063KB > 1953KB） |
| §36 `regress` 結束率 15/15 | 校準前 14/15 → **校準後 15/15，已轉綠** | ⚠ 曾由 F 造成，§9 已修 |

§36 曾是本次**唯一由 Milestone F 造成**的紅燈（一場超過 `regress` 的 30 分鐘
取樣上限）。§9 的收尾校準修掉了它，**校準後重跑 runtime29 為 40/44、§36 綠**。
修法沒有改 `regress` 的取樣上限、沒有放寬 runtime29 的斷言、也沒有全域加速。

剩下的四條紅燈全部是既有問題（§29 v2 陣列順序、§30/§31/§32 Replay 容量串紅），
已列在 `08_目前待辦與風險.md` 的獨立待辦。

另外 §38 `npm run build` 通過；runtime29 的原生公平性不變量本輪也由
`check_moba_pacing29b1` **25/25** 獨立覆蓋（正序 21/40、反序 26/40、位移 13pp ≤ 15）。

## 9. 收尾校準：只處理 §36 那一場（2026-07-30 追加）

唯一沒收尾的是 **seed 42：30.3 分**（regress 取樣上限 30.0 分），只超過 18 秒。

診斷（逐 5 分鐘取樣）：雙方在 25–30 分同時卡在**門牙塔**階段——藍方門牙塔已破、
紅方剩 20 HP，兩座主堡都還是滿血 7200。收尾階段的狀態分布顯示
`攻門牙塔` 878 tick，但換算下來**平均只有 1.4 人在打門牙塔**。

根因不是「推進意願不足」，而是一個**結構性不可能達成的前提**：
`heroTowerSoloK 0.30` 的條件是「沒有己方兵線抵達該建築」，可是
**小兵路線根本沒有延伸進基地廣場**（見 LogicEngine 建構子 `nexus_guard` 的 `t` 註解）
⇒ 門牙塔**永遠**不可能有兵線，於是全程吃 0.30 的懲罰。
那個懲罰是為「帶兵推塔」的路上塔設計的，套在門牙塔上等於要求一件做不到的事。

修法（新增 `nexusGuardNoWaveK: 0.62`）：**只有門牙塔**解除「無兵線」懲罰，
沿用既有的群體係數值。**沒有動塔血、沒有動 `heroTowerDmg`、沒有加速任何一座路上塔、
沒有改團戰成立條件／長度／公平性參數，也沒有放寬任何 verifier、取樣上限或斷言。**

### 收尾校準後的完整重跑

| 驗證 | 校準前 | 校準後 |
|---|---|---|
| `regress` 結束率 | 14/15（seed 42 = 30.3 分） | **15/15**（seed 42 = 24.4 分） |
| `regress` 平均時長／擊殺 | 24.7 分／33.9 | 23.5 分／29.8 |
| `regress2` 節奏門檻 | 8/8 | **8/8**（平均 23.9、中位 23.7、最長 29.1 ≤ 32、5 分均等級 2.6） |
| `pacing29b1` 公平性 | 25/25、位移 **13pp** | **25/25、位移 10pp**（正序 21/40、反序 25/40）⇒ 餘裕變大 |
| `check_moba_milestone_f` | 30/30 | **30/30** |
| `milestone_e`／`d_fix3`／`d_fix2`／`d`／`c_fix` | PASS | **全部 PASS** |
| production build | 通過 | **2596 modules、exit 0** |

### 節奏指標沒有退步（40 seeds，正／反序）

| 指標 | E baseline | F（校準前） | **F（校準後）** |
|---|---|---|---|
| 零碎碰撞率 | 0.49／0.51 | 0／0 | **0／0** |
| 團戰中位長度 | 2.0 秒 | 7.25／6.38 | **7.13／6.38 秒** |
| 無目的遊走率 | 0.38／0.37 | 0.20／0.18 | **0.20／0.18** |
| 團戰後硬收益轉化 | 0.23／0.28 | 0.30／0.30 | **0.32／0.31** |
| 機會事件硬收益率 | 0.36／0.35 | 0.37／0.34 | 0.37／0.34（持平） |
| 平均時長 | 25.26／26.23 分 | 25.12／25.04 | 24.66／24.29 分 |

**要知道的一點**：平均時長比 E baseline 短約 1.5 分（25.3 → 24.7）。仍在
`regress2` 的 [14, 26] 區間內、離下界很遠，但這是門牙塔修正的連帶效果——
所有比賽都會經過門牙塔階段，不只 seed 42。

## 10. 是否建議 push

**畫面與硬門檻都可接受**：FPS 中位 61（手機亦同）、沒有過度群聚、沒有卡位、
HUD 無溢出無重疊、團戰後行為確實轉向推塔／回城／追擊；
`regress` 15/15、`regress2` 8/8、`pacing29b1` 25/25（位移 10pp）、
`milestone_f` 30/30、E／D 系列 verifier 全綠、build 通過。

§9 的收尾校準把唯一由 F 造成的紅燈（runtime29 §36）修掉了，
且沒有放寬任何門檻、沒有全域加速。**建議 push。**

剩下的 runtime29 紅燈（§29 v2 陣列順序、§30/§31/§32 Replay 容量串紅）
全部是既有問題，已在 `08_目前待辦與風險.md` 列為獨立待辦。

## 7. 回退

- 只退追擊約束與群體拆塔：revert 對應 commit
- 連同主動權窗與團戰遲滯：再 revert 前一個 commit
- 比對基準：`git show milestone-f-baseline`（＝`2500ae2`）
- 禁止 `git reset --hard`
