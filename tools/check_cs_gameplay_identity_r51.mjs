#!/usr/bin/env node
// R51: measure the second-layer Leadership and Synergy CS gameplay consumers.
// The verifier instruments memory only; production keeps the fixed RNG/scenario.
import { createHash } from "node:crypto";
import { mkdtempSync, readFileSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { dirname, join, resolve } from "node:path";
import { fileURLToPath } from "node:url";
import { createServer } from "vite";
import { changedSeedSummary, clampSummary as rawCalibrationClampSummary, monotonicity, pairedEffect, thresholdCrossing } from "./cs_calibration_measurement.mjs";

const ROOT=resolve(dirname(fileURLToPath(import.meta.url)),"..");
const FPS_FILE=resolve(ROOT,"src/battle/fps/EsportsFPS3D.jsx");
const FPS_MODULE_ID="/src/battle/fps/EsportsFPS3D.jsx";
const FIXED_SEEDS=Object.freeze([3978742910,4200255727,541349949,1011896540,44863398,1878380147,638784133,2852978760,1789562418,3820910912,3991584863,2186970694,951543597,2082574495,474649321,3950420867]);
const MAP_KEY="inferno",T_TACTIC_ID="t_aexec",CT_TACTIC_ID="c_std";
const EVENT_TYPES=new Set(["leadership_second","synergy_second"]);
const SIGNATURE_MARKER="function simulateFps(mapKey,tacticT,tacticCT,seed=42,roster){";
const SIGNATURE_REPLACEMENT="function simulateFps(mapKey,tacticT,tacticCT,seed=42,roster,__measure=null){";
const LEADERSHIP_MARKER="const leadershipSecond=leadershipFollowUpAfterKill(df,ps,target,N);";
const LEADERSHIP_REPLACEMENT=`${LEADERSHIP_MARKER}__measure?.record("leadership_second",{round:rnd+1,sec,victimId:df.id,leaderId:leadershipSecond.leaderId,playerId:leadershipSecond.playerId,leaderRole:leadershipSecond.leaderRole,role:leadershipSecond.role,led:leadershipSecond.led,opportunity:leadershipSecond.opportunity,action:leadershipSecond.action});`;
const SYNERGY_MARKER="const synergySecond=synergyReady?synergyCoverFollowUpRoute(at,synergyPartner,df):null;";
const SYNERGY_REPLACEMENT=`${SYNERGY_MARKER}__measure?.record("synergy_second",{round:rnd+1,sec,attackerId:at.id,partnerId:synergyPartner?.id??null,attackerRole:at.role,partnerRole:synergyPartner?.role??null,coo:synergyPartner?Math.max(persStat(at,"coo"),persStat(synergyPartner,"coo")):null,opportunity:Boolean(synergyPartner),tradeReady:Boolean(synergyReady),action:Boolean(synergySecond)});`;
const RETURN_MARKER="return { EsportsFPS3D, buildMatchResult };";
const RETURN_REPLACEMENT="return { EsportsFPS3D, buildMatchResult, simulateFps, ROSTER, TACTICS_DB, persStat };";
const EXPORT_MARKER="export { EsportsFPS3D, buildMatchResult };";
const EXPORT_REPLACEMENT=[
  "const __CS_R51_TEST_API__ = Object.freeze({",
  "  simulateFps: __FPS3D_MODULE.simulateFps, ROSTER: __FPS3D_MODULE.ROSTER, TACTICS_DB: __FPS3D_MODULE.TACTICS_DB, persStat: __FPS3D_MODULE.persStat,",
  "});",
  "export { EsportsFPS3D, buildMatchResult, __CS_R51_TEST_API__ };",
].join("\n");
const TRANSFORMS=Object.freeze([
  ["signature",SIGNATURE_MARKER,SIGNATURE_REPLACEMENT],
  ["leadership second",LEADERSHIP_MARKER,LEADERSHIP_REPLACEMENT],
  ["synergy second",SYNERGY_MARKER,SYNERGY_REPLACEMENT],
  ["return export",RETURN_MARKER,RETURN_REPLACEMENT],
  ["module export",EXPORT_MARKER,EXPORT_REPLACEMENT],
]);
const TARGETS=Object.freeze({
  leadership:Object.freeze({key:"led",primary:"ct1",targets:["ct1","t5"],delta:8,threshold:90}),
  synergy:Object.freeze({key:"coo",primary:"ct5",targets:["ct5","t2","t4"],delta:8,threshold:90}),
});
let effective;

function gate(ok,code,detail=""){if(!ok&&!(["OPPORTUNITY_COVERAGE","ACTION_COVERAGE"].includes(code))){throw new Error(`[${code}]${detail?` ${detail}`:""}`);}if(!ok)console.error(`[${code}]${detail?` ${detail}`:""}`);}
function calibrationClampSummary(values,min,max){return rawCalibrationClampSummary(values.map(value=>typeof value==="object"?value.raw:value),min,max);}
function sha(value){return createHash("sha256").update(value).digest("hex");}
function json(value){return JSON.stringify(value);}
function clone(value){return structuredClone(value);}
function freeze(value,seen=new Set()){if(!value||typeof value!=="object"||seen.has(value))return value;seen.add(value);for(const child of Object.values(value))freeze(child,seen);return Object.freeze(value);}
function randTokens(source){return source.match(/\brand\s*\(\s*\)/g)??[];}
function inputDigest(mapKey,tTactic,ctTactic,roster){return sha(json({mapKey,tTactic,ctTactic,roster}));}
function itemArg(){const args=process.argv.filter(item=>item.startsWith("--item="));gate(args.length===1,"ITEM_ARG_REQUIRED","run one R51 item at a time");const item=args[0].slice(7);gate(Object.hasOwn(TARGETS,item),"UNKNOWN_ITEM",item);return item;}
function createCollector(){const events=[];return{events,record(type,payload){gate(EVENT_TYPES.has(type),"UNKNOWN_EVENT",type);gate(payload&&typeof payload==="object"&&!Array.isArray(payload),"EVENT_PAYLOAD",type);for(const [key,value] of Object.entries(payload))gate(value===null||["string","number","boolean"].includes(typeof value),"EVENT_FIELD",`${type}:${key}`);events.push(Object.freeze({schema:"CsGameplayIdentityR51Event.v1",type,...payload}));}};}
function validateEvents(events){for(const event of events){gate(event.schema==="CsGameplayIdentityR51Event.v1","EVENT_SCHEMA");if(event.type==="leadership_second")gate(typeof event.opportunity==="boolean"&&typeof event.action==="boolean"&&Number.isFinite(event.led)||event.led===null,"LEADERSHIP_SHAPE");if(event.type==="synergy_second")gate(typeof event.opportunity==="boolean"&&typeof event.tradeReady==="boolean"&&typeof event.action==="boolean"&&(event.coo===null||Number.isFinite(event.coo)),"SYNERGY_SHAPE");}return events;}
function summarize(events,item,targetId){const type=item+"_second";return events.filter(event=>event.type===type).reduce((out,event)=>{
  const involved=item==="leadership"?event.leaderId===targetId:event.attackerId===targetId||event.partnerId===targetId;
  if(!involved)return out;
  if(event.opportunity)out.opportunity++;
  if(event.action){out.actions++;if(item==="leadership"&&event.role)out.roles.add(event.role);if(item==="synergy"){if(event.attackerRole)out.roles.add(event.attackerRole);if(event.partnerRole)out.roles.add(event.partnerRole);}}
  return out;
},{opportunity:0,actions:0,roles:new Set()});}
function normalizeMetric(value){return{opportunity:value.opportunity,actions:value.actions,conversion:value.opportunity?value.actions/value.opportunity:0,roles:[...value.roles].sort()};}
function metric(rows,key){const values=level=>rows[level].map(row=>row.metrics[key]);const result={monotonicity:monotonicity(values("low"),values("baseline"),values("high")),lowBaseline:pairedEffect(values("low"),values("baseline")),highBaseline:pairedEffect(values("high"),values("baseline")),lowHigh:pairedEffect(values("low"),values("high"))};if(key==="value")effective=result;return result;}
function digestRows(rows){return sha(json(Object.fromEntries(Object.entries(rows).map(([level,levelRows])=>[level,levelRows.map(row=>({seed:row.seed,sim:row.strictSimDigest,event:row.eventDigest}))]))));}
function digestRoleRows(rows){return sha(json(Object.fromEntries(Object.entries(rows).map(([targetId,targetRows])=>[targetId,targetRows.map(row=>({seed:row.seed,event:row.eventDigest,metrics:row.metrics}))]))));}
function directLevels(api,baseline,targetId,key,delta){const original=baseline.find(player=>player.id===targetId);gate(original,"TARGET_MISSING",targetId);const values={low:original.stats[key]-delta,baseline:original.stats[key],high:original.stats[key]+delta};for(const value of Object.values(values))gate(value>=1&&value<=99,"TREATMENT_CLAMPED",`${targetId}/${key}/${value}`);return Object.fromEntries(Object.entries(values).map(([level,raw])=>[level,{raw,effective:api.persStat({...original,stats:{...original.stats,[key]:raw}},key)}]));}
function treatmentRoster(baseline,targetId,key,level,delta){const next=clone(baseline),original=baseline.find(player=>player.id===targetId),target=next.find(player=>player.id===targetId);gate(original&&target,"TARGET_MISSING",targetId);const value={low:original.stats[key]-delta,baseline:original.stats[key],high:original.stats[key]+delta}[level];gate(value>=1&&value<=99,"TREATMENT_CLAMPED",`${targetId}/${key}/${level}`);target.stats[key]=value;for(const candidate of next){const before=baseline.find(player=>player.id===candidate.id);gate(before,"ROSTER_PLAYER_MISSING",candidate.id);if(candidate.id===targetId){const a={...before,stats:{...before.stats}},b={...candidate,stats:{...candidate.stats}};delete a.stats[key];delete b.stats[key];gate(json(a)===json(b),"TREATMENT_NON_TARGET_MUTATION",targetId);}else gate(json(candidate)===json(before),"TREATMENT_OTHER_MUTATION",candidate.id);}return freeze(next);}
async function loadApi(currentSource){let seen=0;const tempRoot=mkdtempSync(join(tmpdir(),"esmo-cs-r51-identity-"));let vite=null;try{vite=await createServer({root:ROOT,configFile:false,envFile:false,appType:"custom",logLevel:"error",cacheDir:join(tempRoot,"vite-cache"),optimizeDeps:{noDiscovery:true,include:[]},server:{middlewareMode:true},plugins:[{name:"cs-gameplay-r51-memory",enforce:"pre",transform(code,id){if(resolve(id.split("?")[0]).toLowerCase()!==FPS_FILE.toLowerCase())return null;seen++;gate(code===currentSource,"VITE_SOURCE_MISMATCH");let transformed=currentSource;for(const [name,marker,replacement] of TRANSFORMS){gate(transformed.split(marker).length-1===1,"TRANSFORM_MARKER_COUNT",name);transformed=transformed.replace(marker,replacement);}let roundTrip=transformed;for(const [,marker,replacement] of [...TRANSFORMS].reverse()){gate(roundTrip.split(replacement).length-1===1,"TRANSFORM_REPLACEMENT_COUNT");roundTrip=roundTrip.replace(replacement,marker);}gate(roundTrip===currentSource,"TRANSFORM_NOT_REVERSIBLE");gate(randTokens(transformed).length===randTokens(currentSource).length,"RNG_TOKEN_COUNT_CHANGED");return{code:transformed,map:null};}}]});const loaded=await vite.ssrLoadModule(`${FPS_MODULE_ID}?r51=identity`);gate(seen===1,"TRANSFORM_LOAD_COUNT",String(seen));return loaded.__CS_R51_TEST_API__;}finally{if(vite)await vite.close();rmSync(tempRoot,{recursive:true,force:true});}}
function runArm(api,tTactic,ctTactic,seed,roster,item,targetId,key){const before=inputDigest(MAP_KEY,tTactic,ctTactic,roster),target=roster.find(player=>player.id===targetId);const off=api.simulateFps(MAP_KEY,tTactic,ctTactic,seed,roster);const collector1=createCollector(),on1=api.simulateFps(MAP_KEY,tTactic,ctTactic,seed,roster,collector1);const collector2=createCollector(),on2=api.simulateFps(MAP_KEY,tTactic,ctTactic,seed,roster,collector2);gate(json(off)===json(on1)&&json(off)===json(on2),"INSTRUMENTATION_CHANGED_SIM",`${item}/${targetId}/${seed}`);const events1=validateEvents(collector1.events),events2=validateEvents(collector2.events);gate(json(events1)===json(events2),"EVENT_NON_DETERMINISTIC",`${item}/${targetId}/${seed}`);gate(before===inputDigest(MAP_KEY,tTactic,ctTactic,roster),"SIM_MUTATED_INPUT",`${item}/${targetId}/${seed}`);return{seed,strictSimDigest:sha(json(off)),eventDigest:sha(json(events1)),raw:target.stats[key],effective:api.persStat(target,key),metrics:normalizeMetric(summarize(events1,item,targetId))};}
function runRows(api,tTactic,ctTactic,baseline,item,spec,targetId){return Object.fromEntries(["low","baseline","high"].map(level=>[level,FIXED_SEEDS.map(seed=>runArm(api,tTactic,ctTactic,seed,level==="baseline"?baseline:treatmentRoster(baseline,targetId,spec.key,level,spec.delta),item,targetId,spec.key))]));}
function baselineRows(api,tTactic,ctTactic,baseline,item,spec,primary){return Object.fromEntries(spec.targets.map(targetId=>[targetId,targetId===spec.primary?primary.baseline:FIXED_SEEDS.map(seed=>runArm(api,tTactic,ctTactic,seed,baseline,item,targetId,spec.key))]));}
function coverage(rows){return Object.fromEntries(Object.entries(rows).map(([targetId,targetRows])=>{const total=targetRows.reduce((out,row)=>{out.opportunity+=row.metrics.opportunity;out.actions+=row.metrics.actions;for(const role of row.metrics.roles)out.roles.add(role);return out;},{opportunity:0,actions:0,roles:new Set()});return[targetId,{opportunity:total.opportunity,actions:total.actions,conversion:total.opportunity?total.actions/total.opportunity:0,observedRoles:[...total.roles].sort()}];}));}
function strictMajority(rows){return Object.fromEntries(Object.entries(rows).map(([level,levelRows])=>[level,{seedsWithAction:levelRows.filter(row=>row.metrics.actions>0).length,totalSeeds:FIXED_SEEDS.length,pass:levelRows.filter(row=>row.metrics.actions>0).length>FIXED_SEEDS.length/2}]));}
async function main(){const item=itemArg(),source=readFileSync(FPS_FILE,"utf8"),spec=TARGETS[item];gate(randTokens(source).length===21,"BASELINE_RNG_CALL_SITES",String(randTokens(source).length));for(const marker of ["leadershipFollowUpAfterKill","synergyCoverFollowUpRoute"])gate(source.includes(marker),"PRODUCTION_CONSUMER_MISSING",marker);const api=await loadApi(source);gate(typeof api?.simulateFps==="function"&&Array.isArray(api.ROSTER)&&typeof api.persStat==="function","SIMULATOR_API_MISSING");const map=api.TACTICS_DB[MAP_KEY],tTactic=freeze(clone(map?.t?.find(entry=>entry.id===T_TACTIC_ID))),ctTactic=freeze(clone(map?.ct?.find(entry=>entry.id===CT_TACTIC_ID))),baseline=freeze(clone(api.ROSTER));gate(tTactic?.id===T_TACTIC_ID&&ctTactic?.id===CT_TACTIC_ID&&baseline.length===10,"FIXED_INPUTS");const primary=runRows(api,tTactic,ctTactic,baseline,item,spec,spec.primary),roleRows=baselineRows(api,tTactic,ctTactic,baseline,item,spec,primary),direct={raw:metric({low:primary.low.map(row=>({metrics:{value:row.raw}})),baseline:primary.baseline.map(row=>({metrics:{value:row.raw}})),high:primary.high.map(row=>({metrics:{value:row.raw}}))},"value"),effective:metric({low:primary.low.map(row=>({metrics:{value:row.effective}})),baseline:primary.baseline.map(row=>({metrics:{value:row.effective}})),high:primary.high.map(row=>({metrics:{value:row.effective}}))},"value")},levels=directLevels(api,baseline,spec.primary,spec.key,spec.delta),roleCoverage=coverage(roleRows),primaryDigest=digestRows(primary),roleDigest=digestRoleRows(roleRows),changedSeeds=changedSeedSummary(primary.low.filter((row,index)=>row.strictSimDigest!==primary.baseline[index].strictSimDigest).length+primary.high.filter((row,index)=>row.strictSimDigest!==primary.baseline[index].strictSimDigest).length,FIXED_SEEDS.length*2),baselineTotals=primary.baseline.reduce((out,row)=>{out.opportunity+=row.metrics.opportunity;out.actions+=row.metrics.actions;return out;},{opportunity:0,actions:0}),threshold=thresholdCrossing([primary.low[0].raw,primary.baseline[0].raw,primary.high[0].raw],spec.threshold,"up"),suite={schema:"CsGameplayIdentityR51Suite.v1",item,mapKey:MAP_KEY,tTacticId:T_TACTIC_ID,ctTacticId:CT_TACTIC_ID,seeds:FIXED_SEEDS,rngCallSites:randTokens(source).length,newRng:false,scenarioChanged:false,nineCalibrationChanged:false,productionConsumers:["leadershipFollowUpAfterKill","synergyCoverFollowUpRoute"]},suiteDigest=sha(json(suite));gate(baselineTotals.opportunity>0,"OPPORTUNITY_COVERAGE",item);gate(baselineTotals.actions>0,"ACTION_COVERAGE",item);console.log(`schema: ${suite.schema}`);console.log(`item: ${item}; fixed seeds: ${FIXED_SEEDS.length}; scenario: ${MAP_KEY}/${T_TACTIC_ID}/${CT_TACTIC_ID}`);console.log(`RNG call sites: ${suite.rngCallSites}; new RNG: false`);console.log(`result: ${json({primaryTarget:spec.primary,levels,baseline:baselineTotals,direct,effective,opportunity:metric(primary,"opportunity"),action:metric(primary,"actions"),consumer:metric(primary,"conversion"),strictMajority:strictMajority(primary),threshold,clamp:{raw:calibrationClampSummary(Object.values(levels),1,99),effective:calibrationClampSummary(Object.values(levels).map(value=>value.effective),1,99)},changedSeeds,roleCoverage,primaryDigest,roleDigest})}`);console.log(`suiteDigest: ${suiteDigest}`);console.log("R51 Level 2 opportunity and Level 3 immediate action measured independently; Level 4 remains secondary");console.log("CS Gameplay Identity R51: PASS");}
main().catch(error=>{console.error(`CS Gameplay Identity R51: FAIL ${error?.stack||error}`);process.exitCode=1;});
