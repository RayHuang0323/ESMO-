# 更新日誌（CHANGELOG）

> 格式：每筆記錄日期、版本、類型與內容。類型：決策 / 新增 / 修正 / 重構 / 文件。
> 與 `03_開發路線圖.md`（任務 R0.2-xx）、`09_技術債務清單.md`（債務 TD-xx）交叉對應。

---

## [v0.2 進行中]

### 2026-07-02 — Legacy Migration M04：Hero Detail 英雄圖鑑（唯讀）✅

**類型**：整合
- 新增 `src/HeroDetail.jsx`（獨立元件，未掛載）：路線分頁（上/打/中/下/輔 各 20 位）＋列表＋詳情面板（中英名、稱號、原型、難度★、P/Q/W/E/R 技能名）。
- **資料來源只有 `CHAMPIONS_100`**；import 僅 react 與 `./data/heroes.js`。
- 驗證：存取欄位全屬 CHAMPIONS_100 既有 13 欄（未新增英雄資料）；程式碼零 LogicEngine/useGameStore/snapshot/roster/gameData 相依；Hero Runtime 四檔指紋未變；MobaView3D 仍為 M01 版、GameView 未動（元件未掛載，View 零影響）；括號平衡 OK。
- 掛載留待你決定時機：`{showCodex && <HeroCodex onClose={...} />}`。

---

### 2026-07-02 — Legacy Migration M01 完成；M02/M03 聚合層完成、UI 暫停待決策

**類型**：整合 / 發現
- **M01 英雄名上畫面 ✅**：`MobaView3D.jsx` 三處最小改動——新增 `setLabelText`（320px 寬畫布重繪）；`heroObjs` 記住 `label/side`；`useFrame` 偵測 `store.roster` 到位後把頭頂標籤由職業字換成英雄名（每人只重繪一次，roster 未到前維持職業字 fallback，換場自動換名）。LogicEngine 未動。
- **M02/M03 聚合層 ✅（UI 未做）**：新增 `src/matchStats.js`（純 JS、零相依）——由每幀 snapshot 以 feed 唯一 id 去重累計逐人 K/D、塔倒事件、含 ts 的事件時間軸，跨場自動歸零。以真實引擎驗證：三種子全場聚合擊殺數 == 引擎 bK+rK、塔倒事件數 == 實際倒塔數、新場歸零正確。
- **⚠️ 引擎發現（暫停原因）**：量測 30 場，主線引擎僅產生 2 次擊殺（平均 0.07/場），多數比賽 35 分鐘（模擬）內未分勝負——K/D Scoreboard 會整排 0。原因：血量<25% 撤退 + dt=0.5 傷害節奏。修正需動 `LogicEngine` 數值（本階段明令禁止），故 Scoreboard/Timeline UI 暫停，待決策（/balance 任務或先出塔倒版時間軸）。

**影響檔案**：`src/MobaView3D.jsx`（M01）、`src/matchStats.js`（新增）。`LogicEngine.js`、`GameView.jsx`、Hero Runtime 未動。

---

### 2026-07-02 — 整合：Hero Runtime Integration 第一步（LogicEngine → useLocalServer → store.roster）

**類型**：整合（v0.2 Integration Task #1）
- 正式把 Hero 身份接進主線資料流的第一段：開局時 `useLocalServer` 以與引擎相同的 `seed` 呼叫 `assignRoster(boot.players, seed)`，寫入 `store.roster`。
- `src/useLocalServer.js`：+import `assignRoster`；捕捉 `seed`；`boot` 後呼叫一次 `setRoster(...)`（在 `setInterval` 之前，**非每幀**）。
- `src/useGameStore.js`：`roster` 欄位、`setRoster`、`reset()` 清空（沿用 Hero Mapping Layer 的 store 設計）。
- **未改動**：`LogicEngine.js`（diff 為空，Engine 不認識 Hero）、`GameView.jsx`、`MobaView3D.jsx`、HUD。前置相依 `src/data/heroes.js`、`src/data/heroMapping.js` 需就位。
- 資料流符合先前 Audit：新增邊皆為 Adapter→Data／Adapter→Store（向下、無回指）；三原則維持（Engine 不認識 Hero、Hero 不認識 Engine、Store 為唯一橋接）。

**驗證**：真實 `LogicEngine(seed).snapshot().players`（10 名）→ `assignRoster` → 10 筆 roster，每位玩家對到一位英雄；靜態證明 `setRoster` 僅 1 次且在 `setInterval` 之前（迴圈內只有 `pushFrame`，snapshot 每幀更新）；store roster 生命週期（空→指派→reset 清空）正確。

**尚未開始**：任何 View 消費 roster（Hero Name / Portrait）、Scoreboard、Timeline。

---

### 2026-06-30 — 新增：Hero Mapping Layer（身份映射層）

**類型**：新增（戰鬥零影響）
- 新增 `src/data/heroMapping.js`（純函式、零相依）：`assignRoster(players, seed)` 將 LogicEngine 的 10 名以位置區分的玩家，對應到 `CHAMPIONS_100` 的一位英雄。決定性、同場不重複、位置與路線吻合（`ROLE_TO_LANE`）。另含 `getHero` / `heroName` 讀取輔助。
- `src/useGameStore.js`：新增 `roster` 欄位與 `setRoster`，`reset()` 一併清空（render-state 橋，符合既有架構）。
- `src/useLocalServer.js`：開局以「與該場 LogicEngine 相同的 seed」指派一次 roster（每場穩定、跨場變化）。
- **LogicEngine.js 本次未改動**——身份映射完全在引擎之外，不觸碰任何戰鬥數值或 snapshot 合約。
- 範圍：僅「名稱」身份；頭像、技能資訊為未來工作。未開始 Skill System。

**驗證**：以真實引擎 `boot.players` 指派——10/10 玩家皆有英雄、路線吻合、同場 10 個唯一英雄、決定性、跨 seed 變化、不污染 `CHAMPIONS_100`、無 players 副作用、可 JSON 序列化；**有無 assignRoster，引擎 200 幀快照逐位元一致（戰鬥零影響）**。

**影響檔案**：`src/data/heroMapping.js`（新增）、`src/useGameStore.js`（+roster/setRoster）、`src/useLocalServer.js`（開局指派）。view surfacing（在畫面顯示英雄名）尚未強制套用，留待選用（見 commit 說明）。

---

### 2026-06-30 — 重構：英雄數值資料化　Hero Data Extraction（Hero System 第一步）

**類型**：重構 / 新增（行為零變更）
- 新增 `src/data/heroes.js` 作為主線英雄資料單一真實來源，含兩區塊：
  - **(A) `ROLE_STATS` / `BASE_HP`**：將原本寫死在 `LogicEngine` 建構子的英雄基礎數值抽離成資料表（`tough`、`power`、基礎 HP 600）。**已接入 LogicEngine。**
  - **(B) `CHAMPIONS_100`**：自 Legacy `EsportsGame.jsx` **逐位元**抽出的 100 位英雄名單（id/en/zh/title/arch/lane/color/diff/PQWER，皆為描述性、無戰鬥數值）。目前為**參考資料、未接入模擬**。
- `LogicEngine.js`：僅三處最小改動——新增一行 import；兩條 inline 數值陣列改讀 `ROLE_STATS[role]`；`600` 改用 `BASE_HP`。邏輯一字未動。
- **重要釐清**：原始需求是「LogicEngine 讀 CHAMPIONS_100」，但 `CHAMPIONS_100` 不含任何戰鬥數值、且 LogicEngine 從未使用它。真正可資料化的是 role-based 的 `ROLE_STATS`。故採「ROLES 數值接線 + CHAMPIONS_100 作為參考資料併入」的方案，完全遵守「不新增英雄／不改數值／不改行為」。

**驗證**：6 個種子 × 每局 501 幀，改版與原版 `snapshot()` **逐位元一致**；玩家數維持 10；`ROLE_STATS` 數值與原 inline 相同；`CHAMPIONS_100` 100 筆、id 唯一、欄位完整、無戰鬥數值；snapshot 仍可 JSON 序列化。（離線靜態：本機無網路，未跑瀏覽器；但 snapshot 逐位元相同即保證渲染一致。）

**影響檔案**：`src/data/heroes.js`（新增）、`src/LogicEngine.js`（3 行）。`MobaView3D` / `GameView` 未動（只讀 snapshot，數值在引擎內部）。

---

### 2026-06-30 — 新增：主線建置進入點 src/main.jsx　（R0.2-03 ✅｜TD-04 解決）

**類型**：新增 / 修正
- 新建 `src/main.jsx`，以 React 18 `createRoot` 掛載主線根元件 `<GameView/>`。
- **明確不經過 `App.jsx`**：原本缺少 `main.jsx`，`index.html → /src/main.jsx` 形同斷鏈；且 `App.jsx`（377KB、Three r128 沙盒）不在主線上。新進入點直接掛 GameView。
- **驗證（離線靜態，本機無網路無法 `npm install`／`npm run dev`）**：自 `main.jsx` 遞迴解析本地 import——7 檔全解析（main → GameView → MobaView3D／useLocalServer → useGameStore → LogicEngine → gameData）；四項斷言全 PASS：本地 import 無缺檔、LogicEngine 在鏈上、**主線未走到 App.jsx**、外部相依皆宣告於 `package.json`。純 `.js` 模組 `node --check` 通過。
- 連帶關閉 R0.2-01 殘留項「確認 B 不相依 A」。
- **尚未解除、屬下一任務的啟動阻擋**：`vite_config.js` 需更名為 `vite.config.js`（R0.2-04 / TD-11），否則 Vite 不載入 react plugin、JSX 不轉譯。

**影響檔案**：`src/main.jsx`（新增）。`index.html` 未改（路徑已正確）。

---

### 2026-06-30 — 決策：確立主線血脈　（R0.2-08 ✅｜TD-08 解決）

正式拍板 ESMO 的架構主線：

- **主線（Trunk，唯一長期維護）**：GitHub 上的 **React + Vite + React Three Fiber（Three.js r160）**。
- **Legacy Prototype（不再維護）**：Artifact／沙盒 monolith（`EsportsGame.jsx`、`App.jsx`、`EsportsFPS3D.jsx`），定位為功能與資料的來源庫，只供參考與移植。

此決策的連帶效果（皆無程式碼變更）：
- TD-08（產品定位／主線未定）→ **已解決**。
- TD-01（three.js 版本血脈衝突）→ **大幅消解**：`package.json` 的 r160 對主線本來就正確；legacy 的 r128 舊 API 無需修。
- TD-02、TD-03、TD-09 → 降為 **Legacy 關注**（不再是主線當務之急）。
- TD-07（平行副本）→ **主線部分已解決**（B 為準、A 為來源庫）。

**影響檔案**：`README.md`、`03_開發路線圖.md`、`09_技術債務清單.md`、`04_更新日誌.md`（本檔）。**未變更任何程式碼。**

---

## [v0.1] — 基準

- 完成技術審查，建立 `09_技術債務清單.md`（TD-01～15）與 `03_開發路線圖.md`（v0.2 路線圖）。
- 階段 A（純渲染 UI）部分實作完成。
