# 技術債務清單

## CS P0 Visual Stability

- 狀態：`CLOSED / OWNER_ACCEPTED`
- 已關閉事故：Player identity visibility、Camera recovery loop、Canvas geometry jitter、Periodic Flash/Ghost Frame。
- 保留的低成本 DEV invariant：bounded fIdx coherence counters、bounded camera recovery counters；不含 capture buffer、visual detector、manual mark 或 debug overlay。
- 長期防回歸 gate：Renderer visibility、CS-A2、C2A、C2B、CS23、Camera recovery、`RAF_FIDX_COHERENCE`、`STABLE_CANVAS_GEOMETRY`、180 秒 browser long-run、production build。
- 不得把上述四項重新列為 active P0；若未來出現 regression，先依 `docs/handoff/10_CS_P0_視覺穩定性與防回歸契約.md` 分辨 failure mechanism。

## 非本輪範圍

既有其他 Sprint 的 debt 保持原紀錄，不在 CS P0 closure 中順便修正；特別是 C2C、Map visual overhaul、gameplay balance、Competition UI、Training、R63 known fast-finish。
## 2026-09-21 Hero Skills active technical debt

- **Skill-ON balance gate**: all 100 Q/W/E/R contracts and VFX compile, but the opt-in runtime still needs same-seed balance/pacing evidence before `heroSkillsV1` can be enabled.
- **Replay compatibility**: named skill identity and snapshot paths are covered by the current slices; full replay/old-format compatibility closure remains open.
- **Semantic fidelity**: some source descriptions imply global targeting, stealth, clone, rewind, persistent link/stat mirroring, maze, immunity, or other mechanics not represented by a look-alike primitive. These remain explicit gaps, not hidden fallbacks.
- **Legacy VFX replacement audit**: `role:basic` / `role:power` and positional fallback assets remain temporarily for compatibility/comparison; they have not been mass-deleted.
- **Device validation**: browser gates cover mobile widths, reduced motion, draw/overlap caps, and shader/page errors; real-device FPS, touch, thermal, and GPU-specific behavior remain unmeasured.
## 2026-09-21 Active debt after Hero Skills formal candidate
- Hero Skills legacy VFX audit: old `role:basic`/`role:power` and positional fallback call sites still need a complete usage audit before safe deletion. The formal named-skill path is already available; do not maintain two formal renderers after the compatibility review.
- Mobile visual validation: browser and Node gates pass, but physical-device FPS, thermal behavior, touch interaction, and reduced-motion perception remain unmeasured.
- Replay compatibility review: preserve only the minimum adapter required by existing saved frames/old skill identity; do not let legacy VFX become a new gameplay fallback.
- Existing project waivers (pacing/fingerprint) remain active and unchanged. P0 MOBA Fairness is closed and must not be mixed into this debt list as an open item.
