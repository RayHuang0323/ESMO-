# 08 目前待辦與風險

## J-close Hotfix 2（已完成，未 push）

報告：`review/moba-runtime/j-close-hotfix2/J_CLOSE_HOTFIX2_REPORT.md`；
rollback `j-close-hotfix2-baseline` → `bcd9b3f`。

**修的是 Hotfix 1 造成的阻斷回歸**：Ban/Pick 整頁**沒有任何捲動軸**，
第六列以後的英雄永遠碰不到。根因是 BanPick 根元素只寫 `minHeight:100%`、
沒寫 `height:100%`，在 AppShell 的 `height:min(88vh,760px); overflow:hidden`
固定盒裡長成 2015px 被直接裁掉（390×844 可見 429px ≈ 5.3 列）。
Hotfix 1 拿掉巢狀捲動時假設「整頁單一捲動軸」，但當時是 **0** 個捲動軸。

修法：根元素 `height:100%` ＋ flex column，英雄格 `flex:1 + minHeight:0 +
overflow-y:auto` 成為全頁唯一捲動區。另新增搜尋框、換篩選/關鍵字回頂部、
細捲軸＋底部漸層提示、手勢位移就吃掉 click（滑動不誤選）。
同時移除 Ban/Pick 的兩處「誰克制誰」呈現（`archCounterScore` 與 AI 行為未動）。

驗證：新增 `shot_banpick_hotfix2` **252/252**（六尺寸 × 版面/捲動/手勢/克制移除
＋ 六尺寸各一次完整流程）、`check_moba_milestone_j_close` **35/35**、
`shot_banpick_hotfix1` 四尺寸完整流程、`regress` 15/15、`regress2` 8/8、build exit 0。

### ⚠ 仍未真機驗（連續第六輪）

本輪所有手機尺寸都是桌面瀏覽器 device emulation ＋ CDP 合成觸控事件，**不是真機**。
慣性捲動手感、`-webkit-overflow-scrolling` 實際效果、真實 safe-area 數值、
瀏覽器工具列收合造成的可視高度變化，只有 Android/iOS 真機能判斷。

### 本輪新增的待辦

1. **Hero Codex「對位」頁籤**（本輪刻意不做）。克制資訊從 Ban/Pick 移除後，
   建議放到會主動去查的地方。資料來源考慮 `src/data/heroMatchups.js`
   ——**這個檔案目前不存在**。⚠ 現行 `archCounterScore` 是**定位相性的 7 條規則**，
   不是逐對英雄的對位表；做真正的對位頁籤必須補資料，**不得虛構未定義的克制關係**。
2. **出戰配置展開時英雄格會變矮**：展開多佔 130–160px，在 1366×768 這種矮視窗
   會把英雄格壓到 1–2 列（仍可捲、仍可選）。預設收合，屬使用者主動打開的暫態，
   本輪未處理。
3. **搜尋只比對字串**，沒有注音／拼音／模糊比對。

### ⚠ 制度教訓（第十次同一個病）

**「有沒有溢出」和「捲不捲得動」是兩件事。**
`shot_banpick_hotfix1` §5k 用
`(root.scrollHeight-root.clientHeight)>0 || all.some(e=>e.scrollHeight>e.clientHeight+8)`
判「頁面可縱向捲動」——在 `overflow:hidden` 的祖先下，這個條件在**英雄完全捲不到**
的情況下照樣成立。§5j「巢狀捲動 ≤1」也綠，因為當時是 0 個捲動軸而 0 也 ≤ 1。
**往後捲動相關驗收的最低標準：派真的手勢，看座標有沒有變。**

## J-close Hotfix 1（已完成，未 push）

報告：`review/moba-runtime/j-close-hotfix1/J_CLOSE_HOTFIX1_REPORT.md`；
rollback `j-close-hotfix1-baseline` → `32806cb`。

**修的是 J-close 造成的阻斷回歸**：出戰配置面板常駐展開把英雄選擇格擠到摺線下，
四個尺寸都選不完角。現在摘要預設收合（33px），四尺寸實測：
摘要 top 204、「選擇你的英雄」top 247、英雄格 top 314、**巢狀捲動 0**。

驗證：`shot_banpick_hotfix1` 四尺寸 **190/190**（1920×1080 / 1366×768 / 390×844 / 360×800）、
`check_moba_milestone_j_close` **35/35**（新增 §13a–13c 順序無關窮舉測試）、
`regress2` 8/8、build exit 0。

⚠ **仍未真機驗**：本輪把頭像縮到 34px、位置標籤 10px 高，
**小螢幕實際可讀性與捲動手感只有 Android 真機能判斷**（連續第五輪未驗）。

### 驗收瀏覽器已改 headless
`ESMO_SHOT_HEADED=1` 可切回實體視窗。

## Milestone K0（已完成，未 push）＋ J-close（已部署）

- **J-close 已部署**：commit `3038945`、GitHub Pages 成功、正式網址
  https://rayhuang0323.github.io/ESMO-/ ⇒ **待 Ray 線上與 Android 真機驗收**。
- **K0**：TD-17 解決。報告 `review/moba-runtime/milestone-k0/MILESTONE_K0_REPORT.md`；
  rollback `milestone-k0-baseline` → `3038945`。

### 驗證工具現況（TD-17 已解決）

```
node tools/verify.mjs --list            列出 18 個區段與上次結果
node tools/verify.mjs --only=a,b        單段／多段
node tools/verify.mjs                   全部
node tools/verify.mjs --resume          只跑還沒通過的
```

全套 **556 秒（9.3 分鐘）跑完**（stats28 158s、runtime29 28s；原本 87 分鐘／跑不完）。
根因是**重複執行**：一次 runtime29 產生 63 個子行程，tactic24 被跑 16 遍。
修法是驅動器 ＋ `ESMO_VERIFY_FLAT=1` 閘門，**沒有刪任何斷言、沒有放寬門檻**。

### ⚠ 全套結果：16/18，不是全綠

| 紅燈 | 說明 |
|---|---|
| `experience26 §17` | Replay 單場 2492KB > 2MB（**TD-19**，Ray 已裁決保留）。數字與 J 相同 |
| `runtime29 §29` | **陣列順序影響勝負分佈：位移 20pp（門檻 ≤15pp）**（**TD-21**，新發現） |

**TD-21 是回歸，不是舊病。** S29A 曾把位移從 48pp 修下來並訂 ≤15pp 為長期防線
（見下方「【P0・已修，長期釘住】players 陣列順序決定勝負」）。現在回到 20pp。
**但不是近期造成**：`milestone-e/f/g/h/i/j-baseline` 與 HEAD **七處各跑 40 seeds，
全部都是 20pp**（連 55%/35% 都一樣）⇒ 至少在 Milestone E 之前就回來了；
之所以現在才看到，是因為 runtime29 以前跑不完。可疑區間是 S29A 之後～E 之前
（最可能是 S29B5／Milestone C–D 的世界幾何與導航改動），**尚未二分定位**。
**本輪沒有動門檻、沒有動引擎**（`git diff 3038945 -- src/` 為空）。

## 待辦

1. **TD-21：順序公平性 20pp**。要判定是混沌還是偏差，需加大 seed 數並分側統計；
   修法會動 rng 流 ⇒ 必然改變所有數值並需重新校準全部 verifier。
   **獨立一輪處理，不要和別的改動混在一起。**
2. **Android 實機仍未驗**（G → I-close → J → J-close 連續四輪）。
3. **「追擊」沒有專屬的英雄定位 knob**（H 的八個 knob 涵蓋其餘六項行為）。
4. **既有英文標籤未全面繁中化**（J-close 只改了新增的分路標示）。
5. `check_dash10`（TD-18）仍紅，且**未納入 runner 區段**——既有紅燈且與 K0 目標無關，
   刻意不順手處理。

## 英雄第二定位的資料上限（已量測，供未來參考）

技能文字能支撐的次要標籤上限就是 **12 名**（門檻 ≥2；≥3 是 11、≥4 是 8；
放到 ≥1 會退化成「法師 41、坦克 40」等於沒有分類）。要更多必須**補資料**
（英雄實際數值或「第二擅長位置」欄位），不是調門檻能解決的。

## ⚠ 制度教訓

**（一）verifier 斷言一律驗行為，不要掃關鍵字**，連 UI 區塊也不要綁死單一字串。
已誤判九次：F §19、G §24、H §28、I §14、tactic24 §B（**從 H 起一直假紅**）、
progress25 §16（連鎖）、milestone_g §7、以及 J-close 自己的兩條
（頭像 testid 是條件式寫法、方格尺寸的正則沒容忍空白）。

**（二）驗收腳本不要靠索引猜元素、不要假設面板展開、不要靠取樣運氣。** 已踩六次：
I-close 手機十人面板預設收合、J 的 Timeline 取樣窗太短、J 的手機戰報預設收合、
J-close 在現場戰鬥取樣技能事件（桌機 0 筆／手機 2 筆，同一份程式碼）、
J-close 改到 Replay 播放取樣仍然飄。
**最終解法：能 seek 就 seek、能列舉就列舉**——逐格拖時間軸掃過整場之後，
桌機 13 種、手機 18 種組合，完全決定性。

**（三）改斷言定義時要把實測值印在標籤上。** J §30 把「撤退鎖死」從「瞬間 ≥8 人」
改成「連續 30 秒 ≥8 人」，因為前者測不到它宣稱要測的東西（實測峰值 8 人、
最長連續 2.5 秒＝正常後撤）。判準改了就要讓數據可見。

## Milestone J（已完成，未 push）

召喚師技能真的進引擎。報告：`review/moba-runtime/milestone-j/MILESTONE_J_REPORT.md`；
rollback `milestone-j-baseline` → `5060c02`。

驗證：`check_moba_milestone_j` **39/39**、`shot_milestone_j` 桌機 **23/23** ＋
手機 390×844 **23/23**、`i_close` 44/44、`i/h/g/f/e` 全 PASS、
`regress` 15/15（23.5 分／29.8 擊殺，**與 I-close 逐值相同**）、`regress2` 8/8、build exit 0。

| 目標 | 狀態 |
|---|---|
| 1 Ban/Pick 為唯一資料來源 | ✅ 沿用 I-close，J 加驗證 |
| 2 五路自動分配 | ✅ 沿用 I，J 加驗證 |
| 3 每人兩個召喚師技能有真實效果 | ✅ **本階段主體**，八個技能全部有引擎效果 |
| 4 英雄定位影響戰鬥行為 | ⚠ 沿用 H；**「追擊」無專屬 knob**（見下） |
| 5 賽前配置資訊 | ✅ 沿用 I-close，J 加驗證 |
| 6 顯示與資料一致 | ✅ 修好三處只認得兩個技能的地方 |

## 待辦

1. **「追擊」沒有專屬的英雄定位 knob**。H 的八個 knob 涵蓋站位／交戰距離／
   撤退門檻／保排／支援／目標選擇，追擊只透過 `focusLowHp`／`engageAdj` 間接
   影響。補這個 knob 需要重新校準節奏，**不要和技能層混在同一輪**——兩者都會
   動擊殺數，混了就分不出是誰造成的。
2. **Android 實機仍未驗**（G → I-close → J 連續三輪未驗）。CDP 觸控模擬 ≠ 真手指。
3. **視覺密度**：本輪技能圖示與冷卻只有 390×844 模擬結果，截圖在
   `review/moba-runtime/milestone-j/evidence/`。

## 驗證債（明確不宣稱全綠）

| 項目 | 狀況 |
|---|---|
| `check_moba_stats28`（29 條）／`check_moba_runtime29`（44 條） | I-close 四次嘗試皆因工作階段時間上限被中止（最後一次停在 stats28 第 13b 條且為綠）。**J 未再重試**。它們涵蓋的子項目多數已個別實跑，但**這兩支自身的斷言集仍無結果** |
| `check_dash10` | 既有紅燈（`snapshot 尚未終局` 拋錯），已在 `bfd4557` 重現 ⇒ 非近期改動造成，未處理 |
| `check_moba_experience26 §17` | replay 單場 ≈2492KB > 2MB，Ray 已裁決保留為既有已知問題 |

## 英雄第二定位的資料上限（已量測，供未來參考）

技能文字能支撐的次要標籤上限就是 **12 名**（門檻 ≥2；≥3 是 11、≥4 是 8；
放到 ≥1 會退化成「法師 41、坦克 40」等於沒有分類）。要更多必須**補資料**
（英雄實際數值或「第二擅長位置」欄位），不是調門檻能解決的。

## ⚠ 制度教訓

**（一）verifier 斷言一律驗行為，不要掃關鍵字**，連 UI 區塊也不要綁死單一字串。
已誤判七次：F §19、G §24、H §28、I §14、tactic24 §B（**從 Milestone H 起
一直假紅**）、progress25 §16（連鎖）、milestone_g §7。

**（二）驗收腳本不要靠索引猜元素，也不要假設面板是展開的。** 已踩四次：
I-close 手機十人面板預設收合（索引 0 不是 b1）、J 的 Timeline 取樣窗太短
（12 秒內第一顆懲戒還沒出現）、J 的手機戰報預設收合（只顯示最新一則）。
修法一律是加 `data-testid` 錨點並比對實際抓到的對象，**不是放寬斷言**。

**（三）改斷言定義時要把實測值印在標籤上。** J §30 把「撤退鎖死」從
「瞬間 ≥8 人」改成「連續 30 秒 ≥8 人」，因為前者測不到它宣稱要測的東西
（實測峰值 8 人、最長連續 2.5 秒＝正常後撤）。判準改了就要讓數據可見，
否則下一個人無法判斷這是修正還是放水。

## Milestone I ＋ I-close（已完成，未 push）

I 的六個目標**全部完成**。I-close 收掉了 I 留下的目標 4 下游、目標 5、
以及第一次的正式流程瀏覽器驗收。

- 報告：`review/moba-runtime/milestone-i/MILESTONE_I_REPORT.md`（第一輪）
  ＋ `review/moba-runtime/milestone-i-close/MILESTONE_I_CLOSE_REPORT.md`（收尾）
- rollback：`milestone-i-baseline` → `c2cc699`；`milestone-i-close-baseline` → `bfd4557`
- 驗證：`check_moba_milestone_i_close` **44/44**、`shot_milestone_i` 桌機 **22/22** ＋
  手機 390×844 **22/22**、`milestone_i/h/g/f/e` 全 PASS、`regress` 15/15
  （23.5 分／擊殺 29.8，**與 H／I 逐值相同**）、`regress2` 8/8、build 2601 modules

| 目標 | 狀態 |
|---|---|
| 1 圖鑑分類（法師只有 10 名） | ✅ 圖鑑可見法師 **20**（12 主 + 8 次要），100 名全部可分類 |
| 2 選手／英雄／五路自動分配 | ✅ 窮舉最佳解、決定性、與選取順序無關 |
| 3 Ban/Pick 顯示選手／位置／適性／衝突 | ✅ |
| 4 每人 2 個召喚師技能貫穿全流程 | ✅ 技能長在對戰名單上，Loading／GameView／Result／Replay 讀同一份 |
| 5 賽前配置的英雄來源標示 | ✅ 五種徽章 ＋「不是固定綁定」說明 |
| 6 共用最終 roster/loadout | ✅ 結構上單一來源，不是靠約定同步 |

## 待驗（只有真人／真機能驗）

1. **Android 實機**：CDP 觸控模擬 ≠ 真手指。Milestone G 的手勢修正至今仍未在
   Ray 的實機驗過；I-close 新增的 Loading 技能標籤在小螢幕的可讀性同理。
2. **視覺密度**：Loading 卡片多一行技能標籤、記分板英雄名後多圖示、Lineup 每列
   多一個徽章。截圖在 `review/moba-runtime/milestone-i-close/evidence/`（桌機＋手機各 6 張）。
3. **文案**：五個徽章用詞照 Ray 原話直接用，要不要換說法由 Ray 決定。

## 既有紅燈（非近期改動造成，已在 baseline worktree 重現）

| verifier | 狀況 | 處置 |
|---|---|---|
| `check_dash10` | `snapshotToBattleResult: snapshot 尚未終局` 直接拋錯 | 在 `bfd4557` 已重現 ⇒ 既有。診斷屬獨立工作，未順手重構 |
| `check_moba_experience26 §17` | replay 單場 ≈2492KB > 2MB | Ray 已裁決為既有已知問題。I-close 前後**同為 836 frames ≈ 2492KB**，沒有變差 |

## 英雄第二定位的資料上限（已量測，供未來參考）

Ray 要求「讓更多英雄有第二標籤，Ban/Pick 更多樣」。實測：技能文字能支撐的
次要標籤上限就是 **12 名**（命中門檻 ≥2；≥3 是 11、≥4 是 8；放到 ≥1 會退化成
「法師 41、坦克 40」等於沒有分類）。要更多必須**補資料**：
英雄的實際數值，或一個明確的「第二擅長位置」欄位。不是調門檻能解決的。

## ⚠ 制度教訓（已第五次踩到）

verifier 的「未改 X／不使用 Y」類斷言**一律驗行為，不要用關鍵字掃描**；
**連 UI 區塊也不要綁死單一字串**。已誤判七次：

- F §19（rng 次數前提錯）、G §24（掃到後續 milestone 的註解）、
  H §28（掃到 `mobaNavigation.js` 檔頭的舊「Milestone H.2」）、
  I §14（掃到模組註解裡的「不抽 rng」字樣）
- **tactic24 §B**（掃字面 `start({ tactic })`，Milestone H 起是
  `start({ tactic, roster })` ⇒ **從 H 就一直假紅**）、
  **progress25 §16**（tactic24 的連鎖）、
  **milestone_g §7**（掃完整標題「召喚師技能（即時冷卻）」）

同類型的還有一種：**驗收腳本靠索引猜元素**。I-close 手機首跑紅燈就是這樣來的——
手機十人面板預設收合、只渲染目前那一路，索引 0 不是 b1。修法是加 `data-seat`
比對實際點到的席位，**不是放寬斷言**。

## Milestone H（已完成並部署）— 剩下的人工項目

英雄選擇第一次真的影響對戰（行為層，非傷害），三個呈現問題一併收掉。
`check_moba_milestone_h` 31/31、真瀏覽器 13/13、英雄層關閉 vs baseline 5/5 逐欄相同、
`pacing29b1` 25/25（位移 10pp）、`regress` 15/15、`regress2` 8/8。
報告：`review/moba-runtime/milestone-h/MILESTONE_H_REPORT.md`；
rollback tag `milestone-h-baseline` → `0813594`。

**需要人眼確認的兩件事**：

1. **巴龍修正的實際觀感** —— 本輪截圖在開局階段，沒拍到「多人持續攻擊巴龍」那一刻。
   建議實際打一場看巴龍團，確認不再是一片米白。
2. **英雄定位的手感** —— 數字證明行為變了、公平性沒壞，但「選坦克是不是真的比較前排」
   要長時間看才判斷得出來。

### ⚠ 已診斷但**不在 H 範圍**：3D 名牌看不清楚（Ray 2026-07-31 回報）

Ray 的要求是「**血條優先，名牌稍微看得到就好**」。診斷如下：

- 名牌是**世界尺寸**的 WebGL Plane（`NAMEPLATE.compactWidth = barW × 0.64 ≈ 3.7`
  世界單位寬、`compactHeight ≈ 0.99` 高），會跟英雄一起隨距離縮小 —— 這是 D-fix3
  為了「不讓 DOM 名牌永遠浮在血條上」刻意做的取捨。
- Milestone G 把縮放下限從 372 放寬到 561 之後，拉遠時名牌又更小了。
- 血條不會被遮的保證已經在：名牌 `renderOrder 69` < 血條 `70–72`，血條永遠後畫。

建議修法（未動工）：讓名牌**依相機距離等比放大**以維持大致固定的螢幕高度（設上限），
並讓放大時**向上生長**（底緣不往血條靠），同時把貼圖解析度調高避免放大後糊掉。
這樣「拉遠也看得到、且永遠不壓血條」兩個條件可以同時滿足。

## Milestone G（已完成並部署）— 剩下的人工項目

兩項待辦都已實作並驗證：`check_moba_milestone_g` 30/30、真瀏覽器 20/20、
`regress`／`regress2` 與 Milestone F 逐值相同（純呈現層）。
報告：`review/moba-runtime/milestone-g/MILESTONE_G_REPORT.md`；
rollback tag `milestone-g-baseline` → `e6325f0`。

**需要 Ray 在 Android 真機確認的兩件事**（桌面觸控模擬證不了）：

1. **手勢跟手手感**：`touch-action:none` 與俯角補償在模擬下量得到「有沒有平移、
   會不會捲動」，但跟手細緻度、慣性、低 FPS 下的表現只有真機能判斷。
2. **真實 pull-to-refresh**：模擬下已證明 `window.scrollY` 不變，
   但真實 Android Chrome 的下拉重新整理仍需真機驗一次。

### ⚠ 只做到一半的一項：390×844 直式的「完整地圖」

縮放下限已從 372 放寬到 **561**（＝相機設計上限 `distMax`），
**縱向已可全覽整張地圖，橫向仍只有約 57%**（需要 977）。
要橫向全覽必須把 `distMax` 拉到約 1040 並放大 `far`，實算 16-bit 深度量化會從
`Δz 0.132` 惡化到 **0.460（約 3.5 倍）**——那正是 H.2-flicker 修的
「Android 單位整批閃爍」成因，**刻意不做**。

處置選項：(a) 全圖總覽用小地圖（現況）；(b) 另開一輪處理相機遠平面與深度精度，
必須在 Android 真機重驗 H.2-flicker。

### Milestone G 殘留

1. **CS（補兵數）仍是佔位**——引擎沒有補兵歸屬，面板顯示「—」（誠實佔位，未編造）。
2. **MP 欄位已隨 3px 直條一起移除**（引擎本來就沒有 mana，舊版是空槽佔位）。
3. 英雄技能只有靜態文字：引擎不模擬個別技能冷卻，故不顯示 CD（面板已明示）。

## Milestone F（已完成，未 push）— 待 Ray 驗收

引擎側完成並重新校準：`check_moba_milestone_f` 30/30、官方公平性
`check_moba_pacing29b1` 25/25（位移 13pp ≤ 15）、`regress2` 節奏 8/8。
報告：`review/moba-runtime/milestone-f/MILESTONE_F_REPORT.md`；
rollback tag `milestone-f-baseline` → `2500ae2`。

正式 GameView 瀏覽器驗收已補做（桌機完整一場 + 390×844）：FPS 中位 61、
群聚平均 2.59（≥6 人只有 4 秒）、無卡位（6.9% 的「幾乎沒動」全是回防／對線／
打野／避塔這類該站著的狀態）、團戰後行為確實轉向圍攻 5.4%／回城 5.9%／追擊 0.9%。

**最需要 Ray 判斷的三件事**：

1. **機會事件硬收益率沒有改善**（0.36 → 0.37）。改善的是無目的遊走率（0.38 → 0.20）
   與團戰型轉化（0.23 → 0.30）。若「每次優勢都要換成硬收益」才算達標，
   需要更長的窗或更強的推進效率 ⇒ 會再動時長與公平性分布，屬另一輪校準。
2. **`runtime29` 分段驗證已跑完：39/44**。五條紅燈中**只有一條是 F 造成的**——
   §36 硬編碼要求 `regress 結束率 15/15`，F 之後是 14/15（一場超過 regress 的
   30 分取樣上限）。硬門檻 `regress2` 的「最長 ≤32 分」是 30.3 分通過、
   `regress` 自身 exit 0。**依指示只記錄、沒有順手重構**。
   另外四條都是既有的：§30/§31/§32 是 Replay 容量串紅；
   §29 是 v2 歷史規則集的陣列順序位移 20pp——Milestone D 報告已記載同一組數字
   並在 baseline 重現過，F 完全沒有動 v1/v2。
3. **正／反序位移 0pp → 13pp**（官方 `pacing29b1` 25/25，門檻 ≤15pp）。
   仍在門檻內但餘裕比 D-fix2 小；機制本身過了行為對稱測試。

### 驗收時看到的既有視覺問題（非 F 造成，未修）

1. **巴龍模型在坑區呈現為一大片淺色平面**；F 讓雙方更常在巴龍坑集結 ⇒ 更常被看到。
2. **手機擊殺橫幅過大**（`Double Kill!`／`Dragon 被擊殺` 蓋到倍率與畫質按鈕）；
   F 讓決勝團戰變多 ⇒ 出現頻率上升。

### Milestone F 殘留技術債

1. **KITE 對近戰仍是「保持 2.2–2.6 單位」**——等於貼身站著，語意不精確。
   本輪刻意沒動（改它會再次移動勝負分布，需另外校準）。
2. **敗方的 `defendUntil` 只寫入未讀取**：目前敗方靠既有的撤退／回防路徑，
   沒有專屬的防守集結。要做需另一輪校準。
3. **主動權窗不考慮兵線狀態**：勝方可能在沒有兵線時去推塔（雖有群體分級補償）。
   接兵線狀態會讓轉化更真實，但也更容易拖長比賽。

## Milestone E（已完成並部署）— 剩下的人工項目

程式、Node 安全網（`check_moba_milestone_e` 49/49）與**正式流程瀏覽器驗收**
（桌機 28/28、390×844 21/21）都已完成；`regress`／`regress2` 與 D-fix3 逐值相同
⇒ 模擬未被動到。證據：`review/moba-runtime/milestone-e/evidence/`。

仍需 Ray 人工確認（工具證不了）：

1. **3D 名牌上的新秀姓名** —— 名牌自 D-fix3 起是 WebGL CanvasTexture，DOM 讀不到、
   診斷探針也沒有這個欄位；本輪兩張 GameView 截圖在比賽鐘 0:15／1:03，英雄還在
   泉水附近，名牌太小。**隊伍面板／Loading／賽後戰報三處已由斷言證明**，只差名牌。
2. Android 真機 FPS、熱降頻、觸控手感、safe area、WebGL driver（D-fix3 清單仍有效）。
3. **是否進 Milestone F**（戰鬥節奏／團戰收益轉化）——F 一定會動勝負分布與
   `regress2` 節奏門檻，必須單獨核准、單獨給回退點。

### Milestone E 驗收時新看到的既有現象（都不是 E 造成，未修，列為候選待辦）

1. **Replay 的戰場呈現預設仍是 legacy**：`MobaReplayScreen` 走
   `loadMapPresentation()`（預設 `legacy`），而正式 GameView 自 H.1 起固定
   runtime-v2 ⇒ 同一場比賽的「現場」與「重播」不是同一套戰場外觀。
   要對齊只需讓 Replay 在 `canUse3DPresentation()` 為真時固定用 runtime-v2，
   但那是呈現行為變更，需要核准。
2. **世界標籤蓋住英雄詳情面板**：`BLUE BUFF · 藍` 等世界標籤壓在
   HeroDetailPanel 上方（見 `06-desktop-talent-in-battle.png`）。屬既有疊層順序問題。

### ⚠ 既有紅燈：單場 Replay 已超過 2MB 容量門檻（不是 Milestone E 造成）

`check_moba_experience26` §17 在 `91904c3` 就已經是紅的。以 `git worktree`
在**未改動的 baseline** 實跑同一支 verifier（同一組 728 frames）：

| 版本 | verifier §17 實測 | 門檻 |
|---|---|---|
| `milestone-e-baseline`（91904c3） | **2063 KB → 紅** | 1953 KB（2,000,000 bytes） |
| Milestone E | 2162 KB → 紅 | 同上 |

- 主因是 **H.3 的小兵欄位 `mn`，單場佔 844 KB**（另以 seed 42／667 frames 獨立量測交叉確認）。
- Milestone E 的 `ps`+`tb` 已用字典索引＋變長列＋條件寫入從 163 KB 壓到 85 KB（+4.8%）。
- **沒有調鬆門檻**——為了綠燈拆警報是禁止的。

**這條紅燈會往上串，是同一個根因不是四個問題**（判讀巢狀 verifier 時請直接看這條）：

```
experience26 §17 replay 容量 ── 紅（baseline 就紅）
  └→ talent27 §31（要求 experience26 全綠）        ── 紅 ⇒ talent27 43/44
       └→ stats28 §20/§21（要求上面兩支全綠）       ── 紅 ⇒ stats28 27/29
            └→ runtime29 §30（要求 stats28 全綠）   ── 必然紅
```

⇒ **`check_moba_runtime29` 在這條紅燈修掉之前不可能是 44/44**，跑它只會多花
2 小時得到同一個結論。Milestone E 改為直接單跑它巢狀的每一支並逐項記錄。

**Ray 已裁決（2026-07-30）：採 (a)** —— 列為既有已知問題，Milestone E 不改 `mn`、
不改 IndexedDB、不動門檻。

**後續獨立待辦（未排期）：Replay 單場容量治理**
- 目標：讓 `check_moba_experience26` §17 回到綠燈，連帶解開 talent27 §31 /
  stats28 §20·§21 / runtime29 §30 這條串紅。
- 候選做法：(b) `mn` delta 壓縮或降精度（會動到 D-fix3 剛驗收過的小兵繞塔精度，
  必須重跑 `check_moba_milestone_d_fix3`）；(c) 改 IndexedDB 並重新設定容量門檻。
- 起手數據：`mn` 844 KB／場、`ps`+`tb` 85 KB／場、其餘約 1.1 MB。
- **在這件事完成之前，`check_moba_runtime29` 不可能是 44/44**，判讀巢狀 verifier
  時請先確認紅的是不是這一條，不要當成新回歸。

### Milestone E 殘留技術債（誠實揭露）

1. **英雄仍不影響對戰數值**——引擎 `loadout` 仍由 `heroProgressStore` 以
   `HERO_ASSIGN` 產生。Ban/Pick 只決定外觀與 `BattleResult.heroId`。要讓英雄
   影響勝負需先設計不違反 S28 §2 紅線的作用點（不得是 damage multiplier）。
2. **紅方仍無選手能力**——AI 對手沒有 profileStore 選手，是天然對照組，
   但也代表藍方平均能力偏高時取得行為面優勢（S28 技術債 3 未變）。
3. **先發指派不檢查位置適配**——新秀可以被指派到任何席位，UI 只標示「本位／
   非本位」，引擎不因此懲罰。要做位置適配懲罰屬平衡改動，需另外核准。
4. **Replay 仍未擷取** `contested`、`recallEvents`、`sp`（召喚師技能）與逐幀
   `feed`；`decision` 只存 action（`score`／`reasons`／`targetId` 是除錯欄位，
   為容量刻意不存）。

## Milestone D-fix3（已完成）— 部署後 Android 人工驗收

程式與 Node／桌面瀏覽器安全網已完成；下一步不是開新 Milestone，而是 Ray 以部署版
在 Android 真機確認以下項目：

1. 正常 1× 觀看塔口蓄能 → 藍／紅單體炮彈逐幀飛行 → 命中爆點、扣血與受擊反應；
   大圓環只能是短提示，不能再次蓋過彈體。
2. 六職業辨識：坦克琥珀、戰士紅橘、刺客紫、法師青、射手金、輔助薄荷；
   近戰扇形／遠程彈體／法術 impact 在多人混戰仍可分辨，且不長期遮場。
3. 英雄頭上只剩縮小名稱／等級與完整血條；不再出現 `D×1`／Buff 秒數文字。
   隊伍面板顯示 `龍×N`、`巴 Ns`、`紅Ns`、`藍Ns`，角色只留低干擾環繞光。
4. 三路觀察小兵經過自家塔：應沿塔外連續繞行，不穿塔、不左右跳邊；敵塔前仍在正常
   攻擊距離停位，不改兵線抵達時間。
5. 紅／藍營地個體 HP／死亡／90 秒重生、Dragon 150 秒、Baron 210 秒與上方 HUD
   同步；Dragon／Baron Buff 在地圖、面板與 Replay 一致。
6. Replay、導播 ON/OFF、單指拖曳、雙指縮放、safe area、WebGL driver、FPS、
   熱降頻與長場穩定性。390×844 證據是桌面 Chrome viewport，不代表真機通過。

若上述有視覺問題，先以正式入口加 `?diag=1&debug=1` 讀事件，再確認
`GameView → MobaRuntimeView3D → adapter → renderer`；不要回退 legacy、不要改公平門檻、
地圖幾何或 Replay contract 來掩蓋呈現問題。

## Sprint 29B6（已完成）— 相機控制 / 真實戰場重播 / 對戰 UI

四個根因都已定位到具體行號並修復（詳見 `05_Sprint紀錄.md` 29B6 節）：
`enablePan={debug}` 導致地圖永遠不能平移、重播是獨立的一套 SVG 地圖、
中立目標與英雄不在同一條插值時間軸、沒人定義過 score header 的安全區。

**最優先：29B6 真機人工驗收**（Ray；手機 320/360/390/430 ＋ 桌機；測試控制加 `?debug=1`）：

1. **地圖操作**：單指拖曳可平移、雙指可縮放、桌機拖曳/滾輪可用；拖到邊界會停住
   （不會滑出黑區）；拖曳/縮放後自動進 free 並出現「🎥 回到導播」；
   點英雄仍 zoom-in 4s；雙擊空白回導播。**地圖不再旋轉**（這是刻意的）。
2. **戰報**：手機左上只有一行 toast，**不再蓋住藍紅勝率條 / 比分 / 時間 / 目標狀態**；
   展開 drawer 仍在資訊列下方；⏩/⚙/倍率/畫質也都在資訊列下方。
3. **中立目標**：Dragon / Baron / 野怪打完時，**血條歸零與模型死亡同時發生**（不再是
   血條還有一截就消失）；淡出約 1.1 秒；死後坑位有一圈環（absent），重生時環消失。
4. **重播**：Result →「▶ 觀看重播」看到的是**真實 3D 戰場**（不是小地圖）；
   播放/暫停/±10s/事件跳轉/倍速/拖曳時間軸都正常；桌機右下有 inset 小地圖；
   **重播不會重複發獎**（回 Result 後金錢/XP 不變）。
   已知：重播**不畫小兵**（frame 未擷取，畫面上有明示）。
5. **效能**：移除 OrbitControls、新增 absent 環與手勢層後的體感 FPS。

**驗收結果決定下一 Sprint 內容。**

殘留風險：

1. `HUD_H = 126` 是**逐列上界推導的常數**，不是執行期量測。改 `BattleHUD` 版型
   （加列／放大字級）時必須同步這個值，否則戰報會再次爬回藍紅條上。
   verifier 29B6 §15 只能斷言 `SAFE_TOP >= HUD_TOP + HUD_H` 與「舊的 top:96/92 沒復活」，
   證不了實際渲染高度。
2. **重播不含小兵**（`MobaReplay.v1` frame 不存；每幀 96 隻會讓 ~345KB 翻倍）。
   若產品要求重播看得到兵線，需擴充契約並重新評估容量（IndexedDB / delta 壓縮）。
3. 重播的 `state` / `respawn` / `contested` / fx / feed / recallEvents 未擷取 ⇒
   重播沒有撤退/回城/團戰徽章、沒有復活倒數秒數（顯示「☠ 陣亡」）。
4. 滾輪縮放以**畫面中心**為錨（非指標錨點）：單次離散事件無法逐幀收斂，
   硬做會因「相機這一幀還是舊 zoom」而漂移。捏合則是逐幀收斂，正常。
5. 觸控 pan 用地面 y=0 raycast，正交投影下是仿射映射、一步收斂；
   **但未經真機驗證**手感（尤其低 FPS 時的跟手程度）。
6. **⚠ 巢狀 verifier 會因主機記憶體不足而假紅（不是回歸，別誤判）**：
   `check_moba_camera_replay29b6` → `runtime29` → `stats28` → `vite build` 時，
   鏈上同時有約 5 個 Node heap ＋ esbuild 的 Go runtime（需 ~1GB）。
   2026-07-16 實測本機 13.84GB 總量、**僅 ~1.7–2.1GB 可用**（Chrome/Dropbox 等佔用）時：
   - `29b6` 完整巢狀 ⇒ `runtime29` 43/44，**`30) Sprint28 verifier` exit=1**；
   - 但**同一支 `stats28` 單跑 29/29 exit 0**（且它沒有 `SKIP_NESTED` 閘門 ⇒ 那是完整跑）；
   - **`runtime29` 單跑 44/44 exit 0**（含 30) stats28 29/29、regress、regress2、build）。
   同樣的程式碼在巢狀深度 2 過、深度 3 不過 ⇒ **資源競爭**，非程式缺陷。
   本 session 亦獨立重現過 esbuild `fatal error: out of memory`（並行 build 時）。
   **判讀方式**：若 29b6 完整模式在 `runtime29` 這一項紅，先看 `runtime29` 單跑；
   單跑綠即為環境問題。建議跑完整鏈前先關掉瀏覽器等大型程式，或直接分兩段跑
   （`SKIP_NESTED=1 node tools/check_moba_camera_replay29b6.mjs` ＋
   `node tools/check_moba_runtime29.mjs`）。
   ⚠ 這個限制**不是 29B6 引入的**——`check_moba_worldscale29b5` 也是同樣的
   `→ runtime29 → stats28` 深度，只是當時記憶體較寬鬆。
7. **pan clamp 夾的是「注視點」，不是「視窗四邊」**（任務單 A-9 的字面要求，
   verifier §4 也是這樣驗的）。注視點推到邊角（例如 (0,0)）時**畫面仍會露出世界外的
   黑區**。要讓視窗邊緣也不出界，clamp 必須是 zoom + 視窗尺寸的函式
   （`halfSpan = viewportPx / zoom / WORLD_SCALE / 2`），會讓 cameraStore 依賴視窗狀態；
   且高 zoom 下必須容許注視點抵達泉水/基地（22,202）等邊角，inset 型 clamp 反而會擋住
   合理操作。**故意保留簡單不變量**，邊角露出多少黑區請 Ray 真機判斷是否需要改成
   zoom-aware clamp（若要改，`clampPan` 需接收 halfSpan 參數，並同步 verifier §4）。

## Sprint 29B5（已完成）— 世界地圖實際放大

- 邏輯世界 100×100 → 220×220；三路弧長約 2.27–2.36 倍，英雄／小兵速度未同比提高。
- bounds、路線、河道、基地、泉水、pits、camps、presentation metadata 集中在 gameData；
  Minimap、Replay、Camera、Objectives 同步。
- 小兵首次受傷 p50 111.5 秒；英雄首次／最早接觸 110 秒；M1 15.5 秒極端值已消失。
- Dragon / Baron / Blue Buff / Red Buff / Jungle Camp 有 distinct 程序化 silhouette / icon；
  未使用第三方官方模型、icon、紋理或截圖作遊戲資產。

殘留風險：

1. reference 影片只讀到 metadata，未擷取畫面；舊 ESMO 影片鏡頭／英雄尺寸／移動節奏未比對。
2. Node 不能證明地圖看起來放大 2–3 倍、手機 FPS/draw calls、觸控鏡頭手感、objective
   美術品質或接近 reference；需 Ray 真機驗收。
3. 新程序化模型仍是 MVP；正式資產須維持 ESMO 自有輪廓，不得引入第三方官方素材。

## 多模型協作風險（2026-07-16 新增）

- 多個 agent（Claude / Codex / 其他）協作可能造成**重複修改、檔案衝突、verifier 未同步**。
- **高風險檔案不得由多個 agent 同時修改**：`LogicEngine` / Store / Replay / contracts
  （`BattleResult.v2` / `CsMatchResult.v1` / `matchProgressTransaction`）。
- **Codex 接手前必須先讀 `AGENTS.md` 與 handoff 文件**（00/05/06/08），並看 `git status` / `git diff`。
- 每個 Sprint 只能有一個 owner agent，只有 owner 可 commit / push（見 `AGENTS.md` §10、
  `docs/ai/跨模型交接流程.md`）。

## 真機人工驗收：29A→29B3 累積清單（仍未驗收；29B6 清單見上，兩者一起做）

Ray 實測（桌機 1366×768、1920×1080；手機 320/360/390/430px；測試控制加 `?debug=1`）：

1. 相機：預設導播、點英雄 zoom-in 4s、拖曳/點空白進自由、雙擊或按鈕回導播、無高頻抖動。
2. 隊伍面板：手機上滑展開完整 5v5／下滑收合、背幕點擊收合、觸控區夠大。
3. 地圖：泉水/基地/三路/河道/龍坑/凱撒坑/營地/草叢一眼可辨；標籤可讀；塔不再過曝。
4. 回城：看得出「引導中→傳送→泉水補血」三段；與走路回家可區分。
5. Debug：`?debug=1` 出現「⏩ 快速完成比賽」，點擊後進 Result/戰報，獎勵不重複。
6. 效能可接受（FPS 無數字基準，體感回報即可）。

**驗收結果決定下一 Sprint 內容。**

## 之後候選（依驗收結果排定）

1. HUD 殘項：z-index 統一常數表、`BattleHeroStrip` per-player selector、
   手機 Scoreboard 入口（TAB 桌機限定）、倍率/畫質併入正式設定面板。
2. 場景正式美術資產（現為程序化/低模＋標籤 MVP）；billboard 標籤距離淡出。
3. timeline 進度條拖曳/快轉到指定時間（需引擎快照回放緩衝設計）。
4. 效能：真機 FPS 基準；若不足，優先檢查 dpr/後製分級與 viewFx/spark 密度。

## Sprint 29B1 殘留技術債

1. **營地不反擊、無入侵**：打野農怪零風險；敵方野區入侵搶怪未實作
   （Smite 搶龍/巴龍**有**實作）。
2. **塔傷不執行擊殺**（最低打到 1 HP）：刻意設計，保 KDA 不變量 Σk==bK+rK==Σd
   （runtime29 §4）；副作用是塔永遠拿不到最後一刀。
3. **5 分鐘擊殺 p50=0 偏安靜**（規格容許 0–4）；前期若要更熱鬧，調 `SIM_RULES.v3`
   的 `chaseHpMax` / `jgSupJoinBonus`（單點）。
4. **其他四路的 D 技能未配置**（明確 reserved）——需先有引擎作用點（治療/護盾/點燃），
   不上「有圖無效果」的假技能。
5. **巴龍 buff 只作用兵線**（拆塔 ×2.2、兵對兵 ×1.7），不加英雄屬性——是收尾機制
   不是強度獎勵；若終局仍偶有拖尾（>32 分），優先調 `baronMinionK`。
6. **v2 規則集保留但不再是預設**——它是 29B1 節奏門檻的檢定力對照組
   （`check_moba_pacing29b1` §2 餵 v2 必須判失衡），**不可刪**。
7. **v3 無戰術藍勝率略低於 50%**（120 場合併約 39–46%，95% CI 寬）：
   verifier 門檻 30–70% 內、且正反序位移僅 3.8pp（非順序偏差）。若後續大樣本
   證實系統性紅方優勢，優先檢查 §P1 的 LANES/WALLS/BUSHES 非鏡像殘留。

## 【P0・已修，長期釘住】players 陣列順序決定勝負

**這是正確性缺陷，不是平衡問題**：模擬結果曾取決於資料結構的擺放順序。
自 Sprint 04 起就存在，被「塔會瞬間融化」的粗暴節奏掩蓋。

實測（40 seeds，v1 = 缺陷版規則集）：把 `players` 陣列反轉，
**藍方勝率 55% → 6.7%**（位移 48 個百分點）。五個根因：

| # | 根因 | v2 修法 |
|---|---|---|
| ① | 傷害立即套用 ⇒ 先被迭代者先出手，被秒殺者連還手機會都沒有 | `simultaneousCombat`：傷害累積 → 一起套用 → 才判定死亡 |
| ② | 移動與交戰同一迴圈 ⇒ 先動的人用敵人**舊位置**判定接戰 | `twoPhaseTick`：全員先移動 → 再全員交戰 |
| ③ | `alive.find` ⇒ 一律集火「索引最小」的敵人 | `nearestTarget`：打**最近**的敵人 |
| ④ | 熱點取「陣列第一個」交戰者的鄰域 ⇒ 永遠繞著藍方長 | `symmetricHot`：取最密集鄰域、並列時 centroid |
| ⑤ | 小兵對戰只迭代藍方 ⇒ 紅兵被集火、系統性先死 | `symmetricMinionCombat`：雙方各自出手、同時結算 |
| ⑥ | `PITS.baron {33,32}` 不在雙方基地中垂線上 ⇒ 紅方近 2.45 單位 | 移到 `{33,34}`（差 0.20），連同 `OBSTACLES[0]` 水池 |

**長期防線**：`check_moba_runtime29` §24–29 的**正序／反序 invariant**
（傷害同時結算 / 小兵雙方迭代 / 最近目標選取 / 地圖對稱 / 整場勝率位移），
每一條都同時跑 v1 對照 —— **v1 的系統性翻轉必須被測出來**，否則測試等於沒檢定力。

⚠ **殘留（P1）**：v2 反轉陣列後**逐 seed 勝方仍可能改變**——rng 抽樣順序跟著 players
迭代順序走（特效骰、無戰術時的參戰骰）。那是**混沌**不是**偏差**（已證明無系統性
陣營優勢）。要做到「反轉後逐位元相同」需改 **per-player rng 流**（seed + player id 派生）；
會改變所有 v2 數值 ⇒ 需重新校準並重跑全部 verifier，**不在 29A 範圍**。

⚠ **殘留（P1）**：**換命時的擊殺 XP 歸屬仍看陣列順序**。`pendingHits` 先結算的那位攻擊者
拿得到擊殺 XP；後結算的那位此時已被標記 `dead` ⇒ `_addXp` 的 `p.dead` 守衛讓他拿不到。
實測同一換命場景：正序 b1 升 Lv2、反序改成 r1 升 Lv2。**不影響勝負**（雙方各記 1 殺，
`runtime29` §24 已驗），影響的是單一次換命的 XP。修法是讓 `_resolveKill` 的 XP 發放不受
攻擊者當下死活影響 —— 會改變所有 v2 數值 ⇒ 需重新校準並重跑全部 verifier，不在 29A 範圍。

⚠ **殘留（P1）**：`LANES` 折線、`WALLS`、`BUSHES` 仍不是精確 180° 鏡像
（例：藍方泉水到下路線頭 7.8 單位、紅方到另一端 11.2 單位，差 3.4）。
已修的是引擎機制真正依賴的部分（基地/泉水、塔與主堡路線參數、小兵出生、雙坑等距）。

## Sprint 29A 殘留技術債（29B1 處置後）

1. ~~終局擊殺數偏高~~ → **29B1 已解**（交戰狀態機；終局 p50 71 → 16）。
2. ~~撤退鎖死 13/15~~ → **29B1 已解**（撤退撤得活 + RECALL；不再有 8 人卡撤退常態）。
3. ~~打野無野區營地~~ → **29B1 已解**（`gameData.CAMPS` 6 座 + FARM 行為）。
4. **播報只有我方（藍隊）**——紅方是 AI，無 roster/個性來源，不虛構。（未變）

## Sprint 29A 改動的既有 verifier（三支，皆為「斷言本身壞掉」而非「擋住 S29」）

1. **`regress2`：舊斷言「15–25 分達標 19/20」→ 8 條節奏門檻**（失敗即 exit 1）。
   舊斷言不只是量身訂做，而是**對病灶零檢定力**：v1（5 分鐘倒 8.8 座塔、全場 Lv1 的
   壞引擎）時長 15.4–21.4 分 ⇒「15–25 分達標」**20/20 全中**。新門檻取自 60 seeds
   分布，其中「5 分塔數 ≤3」「5 分均等級 ≥3」兩條能把 v1 直接判掉。
   依據表：`docs/design/MOBA對戰執行與時間系統.md` §7。
2. **`check_moba_stats28` §13：終局 `power`/`maxHp` 相同 → Lv1／同等級雙錨點**（並新增 §13b）。
   S29 加入本場等級後，天賦改變行為 → 影響升級速度 → 終局 `power` 本來就會不同
   （「打得好所以升得快」）。紅線改為：`basePower`/`tough`/`baseMaxHp` 不被天賦寫入，
   且**同席位同 `mlv` ⇒ `power`/`maxHp` 逐值相同**（實測 4 配置 × 2370 取樣、涵蓋 Lv1–18、
   179 組 `(id,mlv)`、衝突 0）。
3. **`check_moba_tactic24` C4：`dragonContests` 4-seed 單點 → objRate 40-seed 多指標**。
   `dragonContests` 一場只累加「該側有沒有人靠近過坑」，而打野/輔助**無條件**去熱點
   ⇒ **飽和**：m1 4.95 vs m4 5.08（Cohen d = **0.09**，零鑑別力），4 seeds 過關純屬運氣。
   改用未飽和的 `objRate`（坑存活期間坑邊平均人數）：m1 0.435 vs m4 0.548
   （**×1.26**、d = 0.83），並加「逐 seed 勝出 ≥60%」與「8 套戰術 knob→行為 Spearman ρ ≥0.4」。
4. **`check_talent27` §29：`git diff HEAD -- LogicEngine.js` → 天賦/引擎零耦合**。
   舊斷言的真實語意是「LogicEngine 沒有**未 commit** 的改動」：任何 commit 後自動變綠
   （即使該 commit 重寫了引擎），任何合法擴充引擎的 sprint 進行中必然變紅
   （S28 加 `configurePlayers` 也一樣）——它偵測不到它宣稱要偵測的事，
   還讓「commit 前跑全綠」在數學上不可能。改為 commit 無關的雙向耦合判準。

## 當前最高優先問題

（無阻斷性問題。S19 入口、S20 Draft/Result、S22 CS Audit、S23 CS 流程、
S24 戰術進引擎、S25 賽後結算統一、S26 對戰體驗四修、S27 選手天賦系統、
S28 MOBA 能力注入，均已結案。能力閉環自 S28 起**兩邊都成立**：
發放（S25）→ 消費（S27 天賦樹）→ derived stats → **CS 引擎與 MOBA 引擎都吃到**。）

下一步候選：**團戰收益轉化**（見 S28 技術債 2 的根因）、
**商店 / 經營儀表板**（Dashboard 僅剩的兩個誠實佔位）。

## Sprint 28 殘留技術債（MOBA 能力注入）

1. **accuracy / learning 未映射到 MOBA 引擎**（誠實揭露，非遺漏）——引擎唯一
   「精準」的表面是傷害（`dmgAmt = p.power * dt * 0.92`），注入即 damage
   multiplier ＝ 違反紅線；learning 無跨場成長迴圈可掛。兩者在 **CS 引擎與
   戰術適性仍完整生效**。要在 MOBA 生效需先有「交戰命中/失誤模型」。
2. **平衡風險：團隊天賦單投 = 負回報**（實測 160 seeds，兩次取樣皆負）——
   團隊天賦讓全隊更常參與團戰，但本引擎勝負來自**主堡血量**（靠兵線與推塔破），
   「打團打得多」不必然轉化為勝利 ⇒ 團隊天賦（b3）藍勝率 44% vs 無天賦 49%。
   **這是引擎平衡屬性，不是注入層 bug**。刻意不調權重去救勝率（那是對著勝率
   調參）。正解：讓團戰勝利真的轉化為推進收益（Sprint 29 候選 1）。
3. **紅方（AI 對手）永遠中性**——無 profileStore 選手 ⇒ 全隊中性能力，是天然
   對照組，但也意味藍方能力平均高於 70 分時取得行為面優勢。要對稱需 AI 對手
   也有選手能力（＝ AI Teams）。
4. **引擎席位固定 b1–b5**——招募的新秀（id = `r`+timestamp）無法上場注入；
   缺「先發配置 → 引擎席位」的指派系統。
5. **power/tough 仍與選手無關**（刻意）——Phase 13 的 `calcMobaPower` 未採用，
   因為 `p.power` 直接乘進傷害式。若未來要讓實力影響戰鬥數值，需先有不違反
   紅線的設計（例如影響命中/失誤而非傷害倍率）。

## Sprint 27 殘留技術債（選手天賦）

1. ~~**MOBA 對戰輸出不受天賦影響**~~ → **S28 已關閉**（能力經
   `mobaPlayerStats` → `engine.configurePlayers` 進引擎行為層）。
2. **無重置系統**（刻意）——正式 UI 無重置鈕；未來以洗點道具/收費重置恢復
   （方案見 docs/design/選手天賦與能力成長系統.md 末節）。
3. **平衡觀察項**——單項加成上限 +6；多名滿配選手疊加時需在 Season
   引入對手成長對衡（未來 Sprint）。
4. **天賦畫面未經真機瀏覽器實測**（**S28 仍未完成**）——本環境無瀏覽器，僅靜態
   防線驗證。需人工點一輪：PlayerDetail → 天賦 → 購買確認 → receipt → reload
   後保留 → Dashboard 天賦入口 → 320/360/390/430/768/1366/1920 響應式。

## Sprint 26 殘留技術債（對戰體驗）

1. **重播僅存 session 記憶體最近一場**——localStorage 配額不適合 frames
   （345KB/場），重整即失、Result 顯示「無法重播」。要多場持久化 →
   IndexedDB + delta 壓縮（見 docs/design/MOBA重播系統.md 未來節）。
2. **重播是 2D 俯視**——3D 重播需要播放管線與 live 管線（useGameStore/
   useBattleFeed）完全隔離，否則會誤觸發終局結算；列未來項。
3. **手機各檔位未經真機實測**（本環境無瀏覽器）——B 修復只有靜態防線
   驗證（sticky footer / 寬度防護 / grid min()），320/360/390/430/768/1366/1920
   需人工點一輪。
4. **「最近使用英雄」未實作**——profileStore 沒有真實 lastHero 資料來源；
   若產品要在選手卡顯示，需先在賽後結算寫入真實的最近英雄紀錄（標示「最近使用」）。

## Sprint 25 殘留技術債（賽後結算與選手成長）

1. **MOBA matchId 是內容雜湊** —— `BattleResult.v2` 沒有 matchId 也沒有時間戳，
   契約凍結不能加，因此 `mobaMatchId()` 以比賽內容做 FNV-1a 決定性雜湊。
   影響：兩場「完全相同」的比賽（勝方/時長/比分/10 人 KDA・金錢・傷害全等）
   會被視為同一場 → 第二場不發獎。機率極低，且比 seasonStore 現有的 4 欄位
   `resultKey` **更安全**。根治方式：讓 BattleResult 帶 matchId（**需 Ray 核准改契約**）。
2. **CS 團隊獎金略高於 MOBA**（fixture：43 萬 vs 33 萬，1.3×）。
   因 CS 的 marginF 用回合差（16:9 → 7/8），MOBA 固定 3/8。
   在容忍範圍內，Legacy 公式凍結，**本 Sprint 未重新平衡**，列為觀察項。
3. **聲望（reputation）永遠 0** —— 無經 Legacy 驗證的公式，不編造。
4. ~~**天賦點只進不出**~~ —— **S27 已結案**（每選手天賦樹，見上）。
5. **team.lv/xp 刻度仍未統一**（「萬 XP」展示刻度，與 xpGain 50/20 不符）。
   S25 刻意不碰：等級閉環做在「選手」層（交易契約的 teamRewards 本來就只有
   money / fans / reputation，沒有 xp）。
6. **老將升級極慢**（lv38→39 需 1950 XP ≈ 25–40 場）。刻意設計，若體感太慢
   需調 `xpRequiredForLevel`。
7. `check_mount09` 仍失效（檢查 Sprint09 時代畫面，非現役）。
   （`check_flow09` / `check_dash10` 的失效 import 路徑已於 S25 修復並復活。）

## Sprint 21 殘留待辦（經營模組）

本 Sprint 恢復了八個經營模組，以下為刻意未做、需後續處理的項目：

1. **賽後金流未回寫財務** —— `matchRecorder` 目前不寫 `profileStore.finance`。
   賽事獎金 / 薪資支出仍是 Store 的種子資料，不會因比賽變動。
   下一步：在 matchRecorder 增加財務回寫（注意：不得在經營層重算戰績）。
2. **贊助週收入未結算** —— `activeSponsor.weeksLeft` 不會自動遞減，
   每週收入也未入帳。需要一個「推進週」的時間軸入口（Legacy 靠 advanceDay 連動）。
3. **經營能力值不影響 Battle** —— 訓練提升的是經營端數值；引擎 loadout 仍用預設
   `HERO_ASSIGN`，Battle Balance 凍結。若要讓選手能力影響勝負，屬 Contract / Balance
   變更，需 Ray 核准。
4. **新秀未綁定英雄** —— `signProspect` 後 `heroId = null`（UI 誠實顯示「未綁定英雄」）。
   要讓新秀能上場，需要一個「指派英雄」入口。
5. **轉會市場 / 合約談判** —— Legacy `NegotiationModule` 領域，未恢復。
6. **天賦 / 商店 / 經營儀表板** —— Legacy Talent / Equip / Dash 模組，未恢復，
   Dashboard 仍為誠實佔位 Modal。
7. **粉絲門檻形同虛設** —— `meta.fans` 種子值 128,000 遠高於 Legacy 贊助最高門檻
   （3,000 粉絲），贊助條件實際只由勝場把關。要恢復 Legacy 手感需重新校準種子值。

## Sprint 19 待辦（已完成，保留為紀錄）

### 1. 修正 MOBA 入口

- 不得直接進入單一選手詳細頁。
- 應先看到 5 人賽前配置。

### 2. 5 人配置頁

應顯示：

- TOP
- JUNGLE
- MID
- ADC
- SUPPORT

每個位置應顯示：

- 選手名稱
- 目前英雄
- 定位
- Lv / 適配度 / 簡要狀態
- 可點擊查看詳細

### 3. Draft Presentation 串接

需要完成：

Ban/Pick 選誰  
→ Loading 顯示誰  
→ Battle Hero Strip 顯示誰  
→ Battle HUD / Hero Detail 顯示誰

本階段不要求英雄數值影響 Battle 勝負。

### 4. 戰術串接

戰術選擇應顯示到：

- Loading
- Battle HUD

本階段不要求戰術影響引擎數值。

### 5. 英雄圖片

需要 Audit：

- EsportsGame.jsx 是否有 HERO_IMG / base64 / image map。
- 是否可安全整合進 heroDatabase。
- 是否會造成檔案過大。
- 若不能本 Sprint 接入，要留下明確方案。

## 已知缺資料

- mana
- CS
- 召喚師技能
- 技能 CD
- item
- buff
- chat / caster event

以上可能需要未來 Contract 擴充，未經 Ray 核准不可修改。

## Result / PostMatch 風險（Sprint20 已查核結案）

查核結果：

- `genMatch` 只存在 Legacy `EsportsGame.jsx`，主幹 `src/` 沒有。
- 主幹沒有 `PostMatchDashboard`；賽後畫面就是 `BattleEndScreen`。
- `BattleEndScreen` 自 Sprint09 起只讀 `battleStore.result`（BattleResult v2），
  無假 KDA / 假 MVP / 假結算。

Sprint20 修正的是「Draft 與 Result 英雄不一致」：
BattleResult.players[].heroId 現在由 Ban/Pick 選角決定（draftRoster Adapter），
不再固定回退 ROSTER 預設英雄。

殘留待辦：

- BattleScoreboard / HeroDetailPanel 尚未接英雄圖。
- 英雄圖 base64 內嵌使 bundle +393KB；若要瘦身改 public/ 靜態檔 + 懶載入。
- 英雄數值仍不影響戰鬥（引擎 loadout 用預設 HERO_ASSIGN，Balance 凍結）。

## CS / FPS（Sprint23 已完成完整比賽循環）

已完成（S22 Audit + 接線 → S23 完整流程）：

- Dashboard → CsPrep → 選圖 → 戰術 → Loading → 3D FPS → CsResult → Dashboard 全鏈接通。
- 結果契約 `CsMatchResult.v1`（`platform/contracts/CsMatchResult.js`）；
  入史口 `profileStore.recordCsMatch`（冪等）→ csHistory / finance / fans / 收件匣。
- 引擎 Presentation / Balance 原封；名單 = profileStore 真實選手（fpsRoster Adapter）。

Sprint 23 殘留待辦與風險：

1. **XP 不回寫等級** —— rewards.xp 只記錄在 csHistory；team.lv/xp 是「萬 XP」展示
   刻度，與 Legacy xpGain（50/20）不符。統一刻度前不做假回寫。
2. **CS 對手單一** —— 固定引擎內建 Compulsary；BO1 訓練賽定位。BO3 / 對手池 /
   CS 聯賽（含 Prep 賽程分頁的 AI_TEAMS 領域）→ Sprint 24 候選。
3. **CS 不寫 SeasonStore（刻意）** —— seasonStore 只收 BattleResult.v2（MOBA），
   寫入會污染 MOBA 戰績；未來要做 CS 聯賽需先讓 SeasonStore 支援 mode。
4. **duration 無來源** —— 引擎 MatchResult 不含時長；契約欄位保留 null 待接。
5. **未經瀏覽器實測** —— S23 流程只有 build + 27 項腳本證據（引擎含 JSX 無法
   node 直跑）；六段畫面實際點擊走通需人工或 verifier 檢查。
6. **bundle 1.9MB** —— FPS 引擎 + 英雄圖 base64 皆內嵌；建議動態 import 切分
   CS 路徑 + 英雄圖改 public/ 靜態檔。
7. **非現役驗證腳本** —— `check_flow09 / check_dash10`（import 路徑仍 `./src/`）、
   `check_mount09`（檢查 Sprint09 畫面）在 S23 前即失效；建議清理或修復。

## Milestone C 待人工確認（2026-07-29）

- 正式 GameView 已移除 legacy 地圖切換；仍需人工確認其他除錯／Replay 相容入口沒有
  對玩家暴露舊畫面。
- 需在 1× 動態畫面觀看塔追蹤彈完整弧線、連續鎖定、英雄塔下 threat、小兵四段扣血，
  以及營地巡遊／索敵／攻擊／回營完整循環；截圖不能取代動態手感。
- 十名程序式低模英雄已保留獨立 recipe，但還不是逐名完整 GLB；若要更貼近選角圖案，
  應建立 ESMO 自有角色模型／LOD 管線，不可導入受保護 MOBA 資產。
- compact Replay 尚未保存 camp 每幀動態位置；Live 完整、舊 Replay 相容，但 Replay
  只顯示 metadata／home camp 位置。若未來要保存追擊動作，需另做向後相容 contract 設計。
- 390×844 只完成桌面瀏覽器 viewport。Android 真機 WebGL driver、H.2 閃爍、FPS、
  熱降頻、觸控、safe area 與長場 Replay 仍未實測，不可宣稱真機通過。
