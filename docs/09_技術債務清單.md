# ESMO 技術審查報告 ＆ 技術債務清單

> 基準日期：2026-06-30
> 基準版本：v0.1（重構後 / Stage A 完成）
> 維護方式：每筆債務有穩定 ID（TD-xx）。GitHub issue 標題請以 ID 開頭，例如 `TD-01: three.js 版本對齊`。解決後把「狀態」改為 `已解決` 並填入 commit/PR。

本文件提供任何接手的人或 AI（Claude / GPT / Codex / Gemini）一份「現況快照」。閱讀順序建議：先看「現階段 Top 5」與「架構快照」，再看「技術債務清單」，路線圖（`03_開發路線圖.md`）會引用這裡的 ID。

---

## 零之前：2026-07-31 補記（Milestone J-close）

> 本檔基準日是 2026-06-30，下面 TD-01～15 的判斷仍可參考，但**現役的技術債與
> 驗證債請以 `docs/handoff/08_目前待辦與風險.md` 為準**（那份每個 Milestone 都在更新）。
> 這裡只登記幾筆「跨 Milestone、有明確移除條件」的項目，避免它們只活在 handoff 裡。

| ID | 項目 | 狀態 / 移除條件 |
|---|---|---|
| **TD-16** | **開發用「快速完成戰鬥」按鈕** | 進行中。位置：`src/featureFlags.js` 的 `devFastForward`；UI 在 `src/GameView.jsx`（同時要求 `isDebugMode()`）。<br>**用途**：驗收與手測時不必等 20–30 分鐘的模擬。走的是既有 `useBattleFeed` 終局 → BattleResult → 發獎（transactionId 冪等）→ Replay 定稿同一條路，**不跳頁、不繞過結算**。<br>**關閉**：把 `devFastForward` 改成 `false`（單一位置）。<br>**移除時機**：正式上線前，且驗收腳本改用其他方式推進比賽之後，連功能本體一起刪。 |
| **TD-17** | **巢狀 verifier 跑不完（驗證債）** | ✅ **已解決（2026-07-31，Milestone K0）**。根因不是「斷言太多」而是**重複執行**：展開呼叫圖後，一次 runtime29 會產生 **63 個子行程**（tactic24 ×16、cs23/progress25/regress/regress2 各 ×8、flow09 ×6、experience26 ×4、talent27/build ×2）。修法：新增 `tools/verify.mjs` 分段驅動器（`--list` / `--only` / `--resume`），並在 5 支會 fan-out 的腳本加 `ESMO_VERIFY_FLAT=1` 閘門跳過巢狀子驗證（標成 SKIP、不計入分母）。**全套 18 段現在 9.3 分鐘跑完**（stats28 單跑 158s、runtime29 28s，原本分別是 87 分鐘與跑不完）。沒有刪任何斷言。 |
| **TD-18** | `check_dash10` 既有紅燈 | 未解。`snapshotToBattleResult: snapshot 尚未終局` 直接拋錯，已在 `bfd4557` 的 worktree 重現 ⇒ 非近期改動造成。多半是該腳本的 tick 預算不足以讓引擎收斂，需要單獨診斷。 |
| **TD-19** | 單場 Replay ≈2492KB > 2MB | 已知並接受。Ray 已裁決保留為既有問題；`check_moba_experience26 §17` 會一直紅。要解需要壓縮 frame 或降低取樣率，會動到 `MobaReplay.v1` 的容量假設。 |
| **TD-20** | 文件分層混亂 | 部分解。`docs/*.md` 停在 2026-07-03，實際現況在 `docs/handoff/`。已在 `docs/00_AI專案說明.md` 加上導向表，但兩層長期並存仍有讓人讀到過期資訊的風險。 |
| **TD-21** | **陣列順序影響勝負分佈（公平性回歸）** | 未解，**新發現**。`check_moba_runtime29 §29`：把 `players` 陣列反轉後藍方勝率 55% → 35%（40 seeds），位移 **20pp**，門檻 ≤15pp。<br>**是回歸不是舊病**：S29A 曾修掉五個根因把位移從 48pp 壓下來，並把 ≤15pp 訂為長期防線（見 `08_目前待辦與風險.md`「【P0・已修，長期釘住】」）。<br>**不是近期造成**：在 `milestone-e/f/g/h/i/j-baseline` 與 HEAD **七處各跑 40 seeds，全部都是 20pp**（連 55%/35% 都一樣）⇒ 至少在 Milestone E 之前就回來了。之所以現在才看到，是因為 runtime29 以前跑不完（TD-17）。<br>**可疑區間**：S29A 之後～Milestone E 之前，最可能是 S29B5／Milestone C–D 的世界幾何與導航改動（runtime29 §28 既有註記已提到 S29B5 之後這條指標行為改變）。**尚未二分定位。**<br>**下一步**：加大 seed 數並分側統計，判定是混沌（隨機序列不同）還是偏差（某側系統性佔優）；修法可能要改 per-player rng 流 ⇒ 會改變所有數值並需重新校準全部 verifier。<br>⚠ **不得靠放寬門檻結案。** |
| **TD-22** | **`profileStore.js → DEFAULT.meta.reputation` 是死欄位（已 deprecated，未物理刪除）** | 進行中（2026-08-23 產品裁決）。種子 47，但 `mobaProgressAdapter` 與 `csProgressAdapter` 一律寫 `reputation: 0`，註解自承「聲望無經驗證公式 → 0」⇒ 畫面永遠顯示「聲望 —」。<br>它與粉絲（`meta.fans`）概念重疊，兩個「戰隊聲望純量」並存會造成長期困惑。<br>**裁決：不立即物理刪除。** 改為 product semantics deprecated：新功能不得使用、UI 不再顯示、新 settlement 不得寫入、舊 save 相容欄位暫留。<br>**移除條件**：未來一次正式的 save schema cleanup（不為粉絲系統單獨做 migration）。<br>由 Fan System **F0** 執行語意標記。✅ **F0 已實作**（branch `feature/fan-f0-sanitize`，待併入 main）：settlement 不再寫入、收據不再帶、UI 不再顯示；欄位、種子 47、契約 schema 均未動。<br>規格：`docs/design/粉絲系統架構.md` §4.4。 |
| **TD-23** | **`profileStore.js → meta.fans` 載入時沒有 sanitize** | 未解。載入只做 `{ ...DEFAULT.meta, ...saved.meta }`，沒有任何型別或範圍檢查 ⇒ 壞存檔的 `fans` 可以是 `null` / 字串 / `NaN` / 負數，並一路流進 UI 顯示與贊助資格判定。<br>目前不痛是因為 `reqFans` 全部達標（見 TD-24）⇒ **沒有人在依賴這個數字做判斷**。粉絲一旦真的擋贊助，壞值就會變成可觸發的 bug。<br>✅ **F0 已實作**（branch `feature/fan-f0-sanitize`，待併入 main）：`profileStore.js → sanitizeFans()` 接在唯一的存檔水合點——合法值（含 0）原樣保留、負數夾 0、無法還原的值回退 DEFAULT、小數 floor。由 `tools/check_fan_f0.mjs` 33/33 守住。 |
| **TD-24** | **贊助設計有兩處「寫了但不生效」** | **(a) 已解（F1，branch `feature/fan-f1-eligibility`）／(b) 未解（留給 F4）**。2026-08-23 Audit 發現，兩處都已有最終產品裁決。<br>**(a) `data/playerModel.js → SPONSORS[].reqFans` 被種子值蓋掉**：門檻是 0/500/800/1500/2000/3000，但 `DEFAULT.meta.fans` 是 128,000 ⇒ **六個贊助商的粉絲門檻從開局第一秒就全部達標**，真正在擋人的只有 `reqWins`。「粉絲→解鎖贊助」這條唯一的粉絲玩法是死的。<br>　→ ✅ **F1 已校準**：0 / 100,000 / 150,000 / 170,000 / 185,000 / 200,000（從可達性反推，不是照抄 baseline——玩家一季只打 14 場聯賽，baseline 的 800k 需 30 季以上）。中階 1.1 個良好賽季、頂階 3.5 個良好賽季、一般情境 7.4 季，六條驗收標準全過。證據可重跑：`node tools/fan_calibration.mjs`；由 `tools/check_fan_system.mjs` 21/21 守住。<br>　→ 🔒 **Hard constraint**：第二個可用 Sponsor 的 `reqFans` **≤ 新局起始 `fans`**，否則開局 8 週扶持到期後現金流失控（依據：`platform/economy/sponsors.js → STARTER_SPONSORS` 的算式，到期後回到 −11.7 萬/週）。由 `check_fan_system.mjs` 第 7 條守。<br>**(b) `data/playerModel.js → SPONSORS[].perk` 是純文案**：寫著「訓練效果 +15%」「選手體力恢復 +10%」「選手士氣 +5」「比賽獎金 +10%」，但全 repo 只有 `screens/manage/SponsorScreen.jsx` 兩處讀它，**都只是把字串印在畫面上**。<br>　→ **裁決 A（最終）：F4 移除這些 misleading 文案，Fan v1 不補實作。** 補實作會造出 `fans → 贊助 → perk → 訓練 → 選手數值` 的間接戰力路徑，繞三層一樣破紅線。未來若要重做 Sponsor perk，**另開設計題**。<br>　→ 由 `check_fan_system.mjs` 第 8 條守（`perk` 不得被任何邏輯讀取），且**必須在 F1 就上線**——F1 是「順手把 perk 接上」誘因最大的時刻。<br>⚠ `9109fe4` 帶進的 `data/trainingCalculator.js` 讓「訓練效果 +15%」變得容易接上，這正是必須先把裁決寫死的原因。<br>✅ **(b) 的玩家可見部分已於 F4 清除（2026-08-23，branch `feature/fan-f4-ui`）**：`SponsorScreen` 不再 render `perk`，現役卡改顯示合約狀態、詳情改顯示粉絲／勝場門檻。⚠ **`SPONSORS[].perk` schema 欄位仍保留**（未刪除）；由 `check_fan_ui_f4` §7／§7b 守住「不得再 render」。(a) 已於 F1 校準完成。 |
| **TD-25** | **首頁模式磚的 `audience` 是寫死的假數字** | 未解。`src/screens/DashboardScreen.jsx` 的 mode card `audience` 是 `"2,041"` / `"訓練"` / `"賽季"`，完全不讀任何資料，卻與首頁真實的「12.8萬 支持者」同畫面出現 ⇒ 兩個互相矛盾的數字，玩家看得到。<br>**裁決（2026-08-23）：直接移除，不改接 `meta.fans`。** Fans（戰隊支持者）與 Audience（單場比賽觀眾）語意不同；目前沒有 attendance / audience system，不能拿戰隊粉絲冒充比賽觀眾。<br>✅ **已解決（2026-08-23，F4，branch `feature/fan-f4-ui`）**：三張 mode card 的 `audience` 已從畫面移除，**刻意未改接 `meta.fans`**（Fans 是戰隊支持者、Audience 是單場觀眾，語意不同；沒有 attendance system 就不冒充）。由 `check_fan_ui_f4` §3／§3b 守住「主幹 .jsx 不得再出現 `audience:` 假欄位」。 |
| **TD-26** | **文件引用把行號當契約** | 進行中（2026-08-23 裁決 C）。`9109fe4` 只是在 `src/data/playerModel.js` 頂端多 import 一行，就讓 `SPONSORS` 從 246 行位移到 251 行，使多份文件的引用同時失效。<br>**規則：正式架構文件一律用 `檔案路徑 → symbol / export / field` 引用**（例：`data/playerModel.js → SPONSORS[].reqFans`、`profileStore.js → DEFAULT.meta.fans`、`rewardFormulas.js → teamRewardsFor()`）。行號若保留，只標示為 **audit snapshot**，不得被實作或 verifier 當作依據。<br>**已套用**：`docs/design/粉絲系統架構.md`、本檔 TD-22～TD-25、`docs/handoff/08_目前待辦與風險.md` 的 Fan System 節。<br>**未套用**：其餘既有文件維持原狀，日後編輯到時再順手改，不做全面掃描。 |
| **TD-27** | **Windows 工具鏈把檔案寫成 CRLF，而 repo 是 LF** | 未解，2026-08-23 Fan F0 期間發現。在 Windows 上用編輯工具改檔，整檔行尾會從 LF 翻成 CRLF ⇒ **12 行的改動顯示成 230 行的 diff**（實測：`applyMatchProgress.js` 230 個 CR、`MobaFlowScreens.jsx` 240 個、`RewardReceiptPanel.jsx` 120 個）。<br>repo 的 `core.autocrlf=false` 且**沒有 `.gitattributes`** ⇒ git 逐位元保存，不會自動正規化。後果是 review 看到假的大 diff、真正的改動被淹沒，且兩個平台輪流編輯同一個檔會反覆翻轉。<br>**當次處置**：commit 前 `sed -i 's/\r$//'` 把碰過的檔正規化，並用 `git diff --numstat` 與 `--ignore-cr-at-eol` 比對，兩者數字相同才算乾淨。<br>**根治**：加一份 `.gitattributes`（`* text=auto eol=lf`）。**未在 F0 做**——那會一次性改寫大量檔案的行尾，屬於跨全 repo 的動作，應獨立一輪並單獨驗證。 |
| **TD-28** | **沒有獎金政策的賽事拿不到賽季粉絲** | ✅ **已解決（2026-08-23，Fan F2.1，branch `feature/fan-f2-1-award-policy`）**。修法不是繞過契約，是**補上契約缺的那一種政策**：新增 `competition/awardPolicy.js → FAN_AWARD_POLICY`（只有粉絲、沒有現金），與 `prizePolicy` 平行；驗證器改成「任一政策即合法」，**兩者皆無仍然 fail-closed**。CS 聯賽、MOBA 巡迴站與年度總決賽現在都拿得到名次粉絲且一毛錢都不發。沒有 schema version bump、舊存檔行為逐值相同、`fanPolicyRef` 不從收據反推、M1 migration 未動（`cs_schema_v11` 41/41）。由 `check_fan_system.mjs` **66/66**（含 3 個 sentinel）守住。<br>**原始診斷（保留）**：2026-08-23 Fan F2 發現。`settleCompetitionAwardInState()` 只在 Event 有 `prizePolicy` 時被呼叫，而 CS 聯賽、MOBA 亞洲巡迴站、年度總決賽都刻意 `prizePolicy: null`（獎金級距未定義）⇒ **這些賽事的名次拿不到粉絲**。<br>F2 曾嘗試「只解開粉絲那一側」（無政策時傳空獎金表，錢的行為逐值不變），結果 `check_competition_q4` 68/68 掘紅。根因是 **SeasonState.v2 的明文不變式** `award_without_policy`：「award receipt requires a prize policy reference」（`seasonStateV2.js`）⇒ 沒有政策的 Event 不得存在 award receipt。已完整還原。<br>**2026-08-23 Audit 完成（F2 closure）**：現有契約下 **沒有最小、向後相容的做法**。① `awardReceiptRef` 是 `awardEnvelopeOf()` **從帳本推導**的，只要 `processedCompetitionAwards` 有 entry 就會生出來，「寫帳本但不寫 ref」做不到。② 不變式是刻意的：`seasonStateV2.js:428-437` 明寫「Arbitrary v2 events never infer a policy from a receipt」。③ 「給 CS 聯賽一份空獎金政策」會撞掉四支 gate（`cs_major:148`、`cs_major_honors_award:121`、`cs_season_lifecycle:107`、`q7b_asia_finals:159`），且違反「CS 獎金級距未定義，不得憑空發明」的既有產品決策。<br>**候選方案（皆未動工）**：**B-1**（建議）在 Event 新增 `fanPolicyRef` 與 `prizePolicyRef` 平行，envelope 與驗證器同步改——屬 `Event` / `SeasonState.v2` schema 變更；**B-2** 廣義化 `prizePolicyRef` 為 award policy（不建議，要改四支 gate 且與產品決策衝突）。**獨立一輪處理，不得塞進 F4。** |
| **TD-29** | **選手傷病欄位已 deprecated，未物理刪除** | **已上線（2026-08-25，`b80e13c` → main，Actions run #162 success）**，債務本身**保留**。**ESMO 不採用選手隨機受傷／傷停機制。**Milestone O2 曾實作一整套：賽後決定性抽籤產生 `injured` / `injuryDays`、每日 −1、傷停中不可出賽、名單／首頁／選手頁都會顯示。<br>**已移除的是 gameplay**：產生（`applyMatchWear`）、倒數（`applyDailyRecovery`）、出賽閘門（`matchFitness`）、auto lineup 過濾（`autoFillSquad`）、以及所有 player-facing UI。`playerCondition.js` 不再輸出任何 injury API。<br>**保留的是欄位**：舊存檔的 `players[].injuryDays` / `injured` / `injuryUntil` / `injuryRisk` 仍會被 `migratePlayer` 原樣帶過（它以 `{...p}` 展開，不認得也不刪除未知欄位）。**沒有做 migration，也刻意不做**——清欄位需要碰 save schema，而目前沒有任何行為依賴它，為了美觀單獨開一次 migration 的風險大於收益（同 TD-22 / TD-24(b) 的處置原則）。<br>**移除條件**：未來一次正式的 Player save schema cleanup，連同 TD-22 的 `meta.reputation`、TD-24(b) 的 `SPONSORS[].perk` 一起清。<br>⚠ **不得把「欄位還在」讀成「功能還在」**：由 `tools/check_no_player_injury.mjs` **29/29**（含 4 個 mutation sentinel）守住——重新加回 injury roll、重新把 `injuryDays > 0` 當成不可出賽、UI 重新顯示「傷停」，三者任一都會讓 gate 變紅。<br>⚠ **移除受傷 ≠ 移除選手生命週期**：`age`、`condition`／體力、疲勞、連續出賽、Training v1.1 的年齡係數全部保留且由同一支 gate 反向釘住。年齡推進／巔峰／衰退／退休屬 **Season vNext**，另案設計。 |
| **TD-30** | **DEV Quick Recovery（開發測試便利功能）** | **已上線（2026-08-25，`b80e13c` → main）**，清理債務**保留**。**這不是正式遊戲設計**，是尚未正式上線期間的測試工具。位置：`src/debug/DevQuickRecovery/`（logic.js ＋ index.jsx），掛載點只有一處——`screens/manage/TrainingScreen.jsx` 的 1 個 import ＋ 1 行 JSX；旗標 `featureFlags.js → devQuickRecovery`。<br>**提供**：推進 1 天／推進 3 天／全隊恢復至可出賽。<br>**行為**：推進走既有的 `profileStore.advanceDay()`（同一個時鐘、同一套週結算與賽季日曆；賽程未收尾一樣被擋，**照實顯示原因不強推**）。恢復目標由 `CONDITION.restPerDay` 反覆疊加、以 `isMatchFit()` 判定何時停 ⇒ **沒有硬寫體力數字，也不是一鍵滿血**（0 體力的目標是 16，剛好跨過門檻 15）。<br>**兩層閘門**：`isDebugMode()` ＋ `featureEnabled('devQuickRecovery')`，兩層都成立才顯示。正式／預設模式在 build 產物上實測**完全看不到**。⚠ 與既有的 TD-16 devFastForward 同一條規則：正式 build 上 `?debug=1` 仍打得開——那是刻意保留的驗收入口，**不是漏洞，但也正是上線前要收掉的東西**。<br>**關閉**：旗標改 `false`（單一位置）。**移除**：`rm -rf src/debug/DevQuickRecovery/` ＋ 刪掉那 2 行 ＋ 刪旗標。<br>**移除時機**：**正式商業上線前必須關閉或移除**（release checklist 見 `docs/handoff/08_目前待辦與風險.md`）。<br>由 `tools/check_dev_quick_recovery.mjs` **29/29**（含 4 個 mutation sentinel）與 `tools/browser_check_dev_quick_recovery.mjs`（正式 build ＋ dev server 兩種模式）守住。 |
| **TD-31** | **`?debug=0` 只在「會呼叫 isDebugMode() 的畫面」上才生效** | 未解，2026-08-25 正式站 smoke 期間發現（**既有瑕疵，非該次改動造成**）。`ui/debugMode.js → isDebugMode()` 清除 `esmo_debug` 旗標是 **render 時的副作用**：只有真的呼叫到它的元件（GameView、MatchEntryPanel、MatchQueuePanel、FinanceScreen、BattleEndScreen、DevQuickRecovery）才會執行那段 `removeItem`。<br>**後果**：首頁沒有任何元件會呼叫它 ⇒ **打開 `.../ESMO-/?debug=0` 的首頁並不會關掉測試模式**，旗標原封不動留在 localStorage。要真的關掉，得走到會呼叫它的畫面（例如訓練中心）。<br>**影響**：`devFastForward`（TD-16）與 `devQuickRecovery`（TD-30）共用這條規則 ⇒ **上線關閉 dev 工具不可以依賴 `?debug=0`**，要改 `featureFlags` 的旗標（那是編譯期的，一定生效）。<br>**修法（未動工）**：把 debug 旗標的解析與清除移到啟動點（`main.jsx`）做一次，而不是散在 render 裡。屬於獨立一輪，**本輪只記錄不順手改**——它會動到既有的驗收流程。 |
| **TD-32** | **新秀沒有成長空間（中位 8.4 點）** | ✅ **已解決（2026-08-25，V0B）**：改為「原型 → 起始能力 + 成長空間 → potential = core + room」，成長空間變成**直接生成的量**而非殘值 ⇒ 中位 8.4 → **15.8 點**，主能力釘住率 41.5% → **0.4%**。由 `tools/check_prospect_growth_space_v0b.mjs` 31/31 守住。<br>**原始診斷（保留）**：`data/recruitPool.js → genProspects()` 的 `current = potential × (0.35–0.75)` 之後，per-stat 又做 `min(potential, r×30 + current×0.5 + 5)` 上推 ⇒ **新秀入行時主能力已達潛力的 87.6%（中位）**，成長空間 min 1.6 / 中位 **8.4** / max 34.2 點。<br>**後果**：一半的新秀一輩子只能長 8 點以內，潛力 46 的新秀剩 1.8 點（入行即巔峰）⇒ **「成長期 → 巔峰期」的產品前提不成立**，生命週期分段只是換標籤。<br>**修法（未動工）**：調整 `current` 生成與 per-stat 上推，把中位成長空間拉到 20–30 點。<br>⚠ 會改動既有招募平衡與一批 `check_cs_*` fixture ⇒ **獨立一輪 balance 工作**，不可塞進其他 sprint。量測：`tools/season_vnext_calibration.mjs`。 |
| **TD-33** | **成長是漸近線，潛力永遠到不了** | ✅ **已解決（2026-08-25，Foundation Calibration）**：新增 `platform/progress/potentialSpace.js`（leaf，零 import），把「剩餘空間 → 係數」抽成 `trainingCalculator` 與 `levelGrowth` **共用的一份定義**，並把形狀從線性改成 `(room/ref)^gamma`，**gamma = 0.6**。<br>**為什麼不是原本規劃的 `floorRate`**：加一個平坦下限會讓已經頂到上限的人還在慢慢長，等於用下限蓋掉問題，而且會抹平潛力空間差異。gamma < 1 的冪次曲線在 room = 0 時仍**恰好是 0**（上限仍是硬的），但 `dr/dt ∝ r^0.6` 是**有限時間收斂**——數學上會真的走完。實測：從 60 練到上限 80 需 30 次課程（線性時是漸近線，走不完）。<br>**連帶效果**：新秀主能力空間中位數只有 17.4 點，線性除以 40 讓節流閥入行即只開 **43.5%**；改曲線後打開到 **60.7%**。一般新人 Year 4 關閉率 42.4% → **76.5%**。<br>⚠ 這改變了 Training 的輸出值 ⇒ 版本推進 `training-growth.v1.1` → **v1.2**，四支 gate 的 golden fixture 同步更新（皆在檔內標明是**刻意的期望變更**）。由 `tools/check_foundation_calibration.mjs` **57/57**（含 4 個 mutation sentinel）守住，量測報告 `tools/foundation_calibration.mjs`。 |
| **TD-34** | **世界時間只靠訓練推進；打比賽完全不推進日曆** | ✅ **已解決（V1 前半 2026-08-27／V2 後半 2026-08-26）**。<br>**後半（V2）：凍齡刷素質的洞已補上。** 做法不是「打一場 +1 天」，而是**每日容量**：`worldClock.COMPETITIVE_BLOCK.matchesPerDay = 3`——一個世界日只有 3 場一般競技的容量，打滿要再打就得自己推進日曆。⇒ 刷 XP **必然**要付出世界時間，但競技比賽**本身一天都不加**，所以愛打的人**不會比不打的人老得快**。<br>四種做法實跑比較（`tools/timeblock_calibration.mjs`）：A 每場 +1 天（一年打 100 場多老 100 天，且要在結算裡寫時鐘）／B 每 N 場自動 +1 天（多老 33 天，一樣要寫時鐘）／**C 每日容量（多老 0 天，只要一個計數器）**／D 競技點數條（多老 0 天，但要再養一條與體力平行的疲勞）。<br>容量檢查在 `enqueueMatch`、扣格子在 `applyMatchProgress`（唯一結算入口 ⇒ 直接繼承 `processedMatchTransactions` 的冪等），容量掛在 `meta`（**俱樂部層級**，切換 MOBA/CS 不會各拿一份），跨日**讀取時自動歸零**（不需要重置程式）。<br>由 `tools/check_time_block_v2.mjs` **47/47**（含端到端與 3 個 mutation sentinel）守住。<br>**前半（V1）：世界時間不再被凍住。** 新增 `platform/time/worldClock.js`（37 行實碼，純契約）與 `profileStore.advanceWorldDays(n, { reason })` 具名入口＋理由白名單；訓練中心拿掉「沒有人在訓練就 return」的早退；首頁新增**世界時間卡**（`reason: rest`），**不依賴任何前置條件**。<br>⚠ 實測比原本記載的更嚴重：不是「只靠訓練推進」，是訓練中心那顆按鈕第一行就 `if (training.length === 0) return` ⇒ **不指派訓練，世界完全停住（零）**。<br>**同輪釘住的既有正確行為**：`meta.days` 寫入點仍只有兩處；賽季狀態機完全不寫 `meta`（賽季容器不控制選手時間）；MOBA/CS 取交集不各推各的；賽程↔世界日只有 `absoluteDayOf` 一個換算點；生涯年度 84 天由 `7 × 12` 推導並與 `deriveTime` 同源。<br>由 `tools/check_world_time_v1.mjs` **46/46**（含端到端與 4 個 mutation sentinel）守住。<br>**⚠ V2 把 `WORLD_TIME_COST.competitive` 從 `null`（V1 的明確未定案）定案為 `0`**——成本不在每一場，在每日容量。V1 gate 的 §A5 與 sentinel M-B 已同步更新，理由寫在該處。<br>⚠ 快速練習已確定 **0**（純測試場），正式季賽也是 **0**（賽程日由日曆帶到，不是比賽推日曆）。<br>**原始診斷（保留）**：<br>2026-08-25 Season vNext Audit 發現。**現在就已經存在**，不是 vNext 才有的問題。<br>`profileStore.advanceDay()` 是唯一時鐘推進者，正式 UI 只有訓練中心「推進訓練日」一個入口（且要求至少一人在訓練）；`applyMatchProgress` 只**讀** `meta.days`，從不寫。<br>**後果（exploit）**：玩家可以完全不推進日曆，無限打自由對戰累積 XP → 升級 → `applyLevelGrowth` 永久成長 ⇒ **凍齡刷素質**。年齡系統一旦上線，這個洞會直接讓它失效。<br>**修法**：Season vNext 的 Time Block 制（任何 match 都落在某個 Block 內，Block 消耗 Career Day）。見設計文件 §3.3 與 §11-G2。 |
| **TD-35** | **`MatchProgressTransaction` 不帶 `MatchOrigin`，結算分不出聯賽與自由對戰** | ✅ **已解決（2026-08-25，V0C）**：新增 `platform/progress/matchSource.js`（三層：practice / competitive / official），adapter 用同一支分類把來源寫進 `metadata.matchSource`（附加欄位，舊交易單仍合法），`applyMatchProgress` 讀它交給 PCGM。`fanSourceWeight.js` 零改動（粉絲行為逐值不變），兩支分類器由 gate §X1 釘住不得在「是不是正式季賽」上分歧。四個來源 base 仍一律 1.0——**現在是可獨立控制，數值留給 Foundation Calibration**。由 `tools/check_match_source_v0c.mjs` 21/21 守住。<br>**原始診斷（保留）**：<br>`applyMatchProgress` 收到的 `MatchProgressTransaction` 只有 `mode` / `matchId` / `metadata` / `playerProgress` / `teamRewards`，**沒有 `MatchOrigin`**（`contracts/matchOrigin.js` 的 `ORIGIN_KINDS.ticket` vs `.fixture`）⇒ 賽後結算**無法分辨這場是聯賽還是自由對戰**。<br>**後果**：設計文件 §5.1 的「Formal base 高於 Training，讓正式賽事成為生涯成果」**做不到**。現在調高 `formal` 的 base 等於**自由對戰一起調高** ⇒ 直接製造「刷自由對戰＝刷正式賽成長」的 exploit，正好是設計文件 §11-G1 要擋的東西。<br>**因此 V0A 的 `PCGM_PARAMS.sourceBase` 四個來源一律 1.0**——這是**刻意的，不是還沒填**。<br>**修法（未動工）**：把 `MatchOrigin` 接進結算契約。屬**契約變更**，獨立一輪，不得塞進 V0A/V0B。它是「年度來源比例 target 40/35/15/10」的**前置條件**。 |
| **TD-36** | **`practice` 的倍率被「資料遺失」綁住，調不動** | ✅ **已解決（2026-08-26，V0D）**：`MATCH_SOURCE` 增加第四層 **`unknown`**，`matchSourceFromOrigin(null)` 從 `practice` 改回 `unknown`（`sourceBase.unknown = 1.0`，**永遠中性**）。`practice` 從此只由**明確的 practice origin**（`ORIGIN_KINDS.practice`）產生 ⇒ 可以安全歸零，`sourceBase.practice = 0`。<br>**「查不到來源」與「玩家真的在打練習賽」從此是兩件事。**<br>同一輪一併交付快速練習的產品入口（V0D）：新增第三個 origin 生產者 `matchmaking/practiceGateway.js`（56 行實碼），與 `competitionGateway` 平行——三者呼叫**同一個** `createRoom` / `createSession`，**沒有第二套 battle pipeline，也沒有第二套 settlement**。<br>由 `tools/check_practice_match_v0d.mjs` **67/67**（含端到端實跑與 4 個 mutation sentinel）守住。<br>**原始診斷（保留）**：<br>2026-08-25 Foundation Calibration 的 fallback audit 發現。`matchSourceFromOrigin(null)` 回 `practice`，而快速練習的**入口尚未實作** ⇒ **目前唯一會落到 practice 的是「交易單拿不到 origin」**（debug harness／舊存檔／舊流程）。<br>**後果**：現在把 `practice` 的 base 調低（例如「練習賽不該有永久成長」），實際上是在懲罰**資料遺失**，而且是看不見的——正常比賽偶爾少發成長，玩家與開發者都不會發現。<br>**因此本輪 `sourceBase.practice` 維持 1.0**，與 `competitive` 相同（`check_foundation_calibration.mjs` §S4／§R5 釘住這個理由）。這**不是還沒調**。<br>**Audit 結論（好消息）**：正常路徑不會掉 origin。`createSession` 是唯一的場次工廠，兩個生產者（`mockGateway.openSession` 票券／`competitionGateway.openSessionForFixture` 賽程）都經過它，而它**拿不到來源時硬性拒絕發出場次**（`code: "ticket"`）⇒ 只要 session 存在就一定有 origin。<br>**修法（建議，未動工）**：`ORIGIN_KINDS` 增加 `practice` 這一種，快速練習入口用 **explicit practice origin** 建立場次；同時把「沒有 origin」與「明確是練習」分成兩種東西（例如 fallback 改成中性的 `unknown`，倍率永遠 1.0，只有 explicit practice 可以被調低）。**這是快速練習 UI 的前置條件**，做 V0D 之前必須先做。 |
| **TD-37** | **兩支 CS gate 對 `playerModel.js` 的 SHA 鎖已永久失效** | 未解，2026-08-25 Foundation Calibration 期間確認。`check_cs_learning_lifecycle_r16b.mjs` 與 `check_cs_learning_measurement_r37.mjs` 用 `EXPECTED_SOURCE_SHA256` 硬鎖 7 個檔的 SHA256（含 `src/data/playerModel.js`），檔內註解寫著「Lock after the first evidence review; no update/rebaseline flag exists」。<br>**實測**：`playerModel.js` 在 **HEAD（本輪改動之前）** 的 SHA 就已經與期望值不符（`b02237b6…` vs 期望 `9d6f07f8…`），兩支也同時卡在更早的 `R48_LEGACY_R47_SHA`（FPS 引擎歷史重建）⇒ **是先前就存在的紅燈，不是本輪造成的**。<br>**後果**：`playerModel.js` 是活躍檔（本輪新增一門訓練課程），這組鎖**永遠不可能再綠**，等於兩支 gate 已失去回歸偵測能力。<br>**修法（未動工）**：要嘛設計一次有紀錄的 rebaseline（連同 R48 重建），要嘛把 SHA 鎖換成**語意斷言**（那些 gate 真正在乎的是 learning stat 的正規定義與讀取鏈，不是位元組）。 |
| **TD-38** | **競技比賽在成長面幾乎進不了場（體力經濟，不是倍率）** | 未解，2026-08-25 Foundation Calibration 量測發現。<br>**實測**：認真訓練的選手一年只排得進 **0.8 場**競技比賽 ⇒ 競技來源佔年度成長 **≈ 0%**。原因是訓練與比賽搶**同一份資源**（選手日 ＋ 體力），而 `conditionEfficiency` 會用體力去乘**所有**訓練成長，所以出賽的代價不只是那一天，是之後一段時間的訓練都在打折。<br>**後果**：產品定位「一般／競技比賽 = 有實戰價值」目前**在成長面不成立**。它的價值是錢／粉絲／戰績，不是養成。<br>**為什麼不能用倍率修**：實測 `competitive` base 1.5 時，純刷競技（21 場/年）的 Year 4 關閉率會**反超**認真訓練（81% vs 75%）⇒ 直接做出「刷比賽＝最佳養成法」。1.0 是分界線。<br>**修法（未動工）**：槓桿是**體力經濟**（`CONDITION.matchEnergyCost` / `restPerDay` / `streakEnergyStep`），不是 `sourceBase`。但那會連動輪換、疲勞與賽程密度設計 ⇒ **獨立一輪**，不可塞進成長校準。 |
| **TD-39** | **線上陣容定價與比賽模擬可能吃不同的能力（免費戰力差集）** | 未解，2026-08-26 生涯×線上公平架構 Design Sprint 設計時發現。<br>線上公平模型（CBR 三層）要對陣容定價，設計已強制**委派既有 `competition/teamStrength.v1`**，避免自建第二套戰力公式。但 `teamStrength.js` 檔頭自己承認**未與 LogicEngine 校準**（規格 D16／R10）。<br>**後果**：若未來線上比賽真的用 LogicEngine 打，而定價用 `teamStrength`，兩者涵蓋的能力**差集 = 未被定價的免費戰力**，玩家會全部堆在那裡，陣容上限當場失效。<br>**為什麼不能用「定價再加一點權重」修**：那只是讓兩套公式以不同方式錯，差集依然存在。<br>**修法（未動工）**：校準兩者，或讓定價與模擬共用同一支。**V7（線上契約）之前必須處理。**見 `docs/design/Season_vNext_生涯與線上競技公平架構.md` §6.1／§11-A12。 |
| **TD-40** | **V3–V5 之間，快轉時間幾乎沒有代價** | 未解（有時效），2026-08-26 Design Sprint 發現。<br>時間的代價目前只有一層：`ageEfficiency`（34 歲 = 20 歲的 **29%** 成長效率）。**衰退（V4）與退休（V5）都還不存在** ⇒ 快轉會讓選手變老，但老了不會變弱、也不會離隊。<br>**這不足以阻擋 V3**：`ageEfficiency` 已經讓「無腦快轉到全員滿能力」行不通——等你快轉完，他們已經老到長不動了。<br>**但它是一個有時效的已知缺口**：V4/V5 落地後必須回頭複驗「快轉到底值不值得」，不可預設仍然成立。見設計文件 §11-A10。 |
| **TD-41** | **快轉 + 訓練是通往潛力天花板的主要途徑（生涯側平衡）** | 未解，2026-08-26 Design Sprint 發現。<br>訓練貢獻 **89%** 的成長，只被體力與課程天數限制；快轉推進天數 ⇒ 回體力 ⇒ 可以再練 ⇒ 快轉確實是抵達天花板的主要途徑。<br>**三個阻尼已經存在**：① 潛力硬上限（`potentialSpace` 在 `room = 0` 時**恰好是 0**）② 會變老（TD-40）③ **換不到任何線上優勢**（線上零成長 + 陣容上限）。<br>⚠ **這是生涯側的平衡問題，不是線上公平問題**，不要混在一起修——線上那一側已由公平架構結構性擋住。<br>**修法（未動工）**：V5 之後的 balance 輪次。與 TD-38 的體力經濟同一個槓桿，宜合併處理。 |
| **TD-42** | **競技每日容量「不跨日累積」目前沒有測試釘住** | ✅ **已解決（2026-08-26，V3）**：`tools/check_time_block_v3.mjs` §I7 補上七項斷言——跨 1 天、跳 7 天、跳一整個生涯年度、**掃 1–90 天容量恆等於上限**、同日不誤重置、髒資料不產生負剩餘、規劃器不得宣稱可結轉；端到端另驗「用滿今天 3 格後跳 7 天 ⇒ remaining 仍是 3，不是 21」。sentinel **M-A**（把跨日重置改成 `<=`）⇒ §I7-2 變紅。<br>同輪一併補上 Design Sprint 指定的第二個缺口 **§W 多週結算冪等**：一次跳 3 週 ⇒ 恰好 3 次結算，與「跳 21 次一天」在**結算次數／天數／資金／`lastSettledWeek`** 四項逐值相同；sentinel **M-B**（跨週判定改成 `>=`）⇒ §W1 變紅。<br>⚠ **兩組在實作前就已經是綠的**——行為一直正確，只是沒有測試釘住。V3 讓天數可以被大量跳過，這兩個洞才真的有人會踩到。<br>**原始診斷（保留）**：<br>2026-08-26 Design Sprint 查核發現。<br>V3（大顆時間操作）一次推多天時，玩家會不會拿到 `7 × 3 = 21` 場競技容量？<br>**查過現況：不會。** `worldClock.competitiveBlockOf(stored, day)` 在 `stored.day !== today` 時 `used` 歸 0，容量恆為 `COMPETITIVE_BLOCK.matchesPerDay = 3` ⇒ 跳過的日子**不會被銀行化**。<br>⚠ **但這是碰巧正確**——`check_time_block_v2` 的 47 項裡沒有任何一項釘住它。任何人「順手」加一個「把沒用完的容量結轉」的善意功能就會破掉，而且不會有 gate 變紅。<br>**修法**：V3 補 sentinel「**讓容量跨日累積 ⇒ gate 變紅**」，並補「一次跳多週時，週結算冪等鍵仍成立」（`check_time_block_v2` §F1 只驗過單週）。見設計文件 §13.1。 |
| **TD-44** | **打完一場「快速練習」之後，賽前頁永久回不到一般對戰** | ✅ **已解決（2026-08-28，`b1830b3`）**：終局判定抽成 `contracts/matchFlowIdle.js`，由 `canStartPracticeFrom()` 與 `matchPracticeContext()` **共用同一份**；並把旗標拆成兩個——`inPractice`（這場的來源是不是練習，**結算端用，語意不動**）與 `activePractice`（現在還在不在練習流程裡，賽前頁用）。⚠ 不能只改一個旗標：`progress/settleCsMatch.js` 第 4 步（入史）的讀取時機**在 `settleMatchThroughSession` 之後**，那時場次已 `completed`，把終局判定加進 `inPractice` 會讓練習賽開始寫 CS 戰績。⚠ `activePractice` 刻意**不看** `practiceAssignment`（沒有任何流程會清掉它）——算進去的話，玩家按「重新配對」開一場真的競技比賽時橫幅會寫「快速練習」，那是比 TD-44 更危險的反向誤導。gate：`check_td44_practice_exit` **46/46**（含把共用判定改壞再 import 回來的真 sentinel M-A）＋ `browser_check_td44_practice_exit` **33/33**（MOBA 與 CS 各走一遍：練習完成 → 名稱回來 → 容量回來 → 仍能再開練習 → reload → 推 1 天）；正式站 smoke `browser_check_prod_v7_release` **44/44**（P7–P10 就是它的迴歸測試）。<br>**原始診斷（保留）**：2026-08-28 V7 正式站 smoke（當時以 W1 印出、不計分）實測發現。<br>**症狀**：練習結算完成後，MOBA **與 CS** 的賽前頁都停在 `tier=practice`，主按鈕只剩「重新開始快速練習」；一般對戰的名稱與今日 N/3 容量再也不顯示。重整、推進 1 天、再推 7 天（第 9 → 17 天）都清不掉。<br>**根因**：`profileStore.matchPracticeContext()` 的 `inPractice` 只看 `session／room／practiceAssignment` 的 `origin.kind`，**沒有**像 `matchPrepAction.canStartPracticeFrom()` 那樣把**終局場次**視為閒置；練習打完之後殘留 `session=completed/practice` ＋ `room=confirmed/practice` ⇒ `inPractice` 永遠為真。<br>⚠ **不是 V7 引入的**：`4652c00`（2026-08-25）就存在，早於本次 release 的基準 `516613f`。V7A 新增的層級橫幅只是讓它變得看得見。<br>**修法（未動工）**：把 `canStartPracticeFrom` 的終局判定抽成共用述詞，`inPractice` 也套同一份；動的是流程狀態機，需要自己的 gate 與 sentinel ⇒ 另開一輪。 |

> ⚠ **編號重複待整理**：`docs/handoff/05_Sprint紀錄.md` 的 V7 那一節自行編了 **TD-42（俱樂部點數沒有兌換出口）** 與 **TD-43（週目標少一個「不同戰術」）**，其中 TD-42 與本表既有的 TD-42 撞號。本次 release 不重編（避免動到已發佈的紀錄），下一輪整理技術債時一併處理。

---

## 零、現階段 Top 5（決策後）

> 重要：2026-06-30 正式拍板**主線血脈**——GitHub 上的 **React + Vite + R3F（three r160）即 ESMO 唯一主線**；Artifact／沙盒 monolith 定位為 **Legacy Prototype**，只供參考與功能移植，不再長期維護。
> 這個決策本身就重排了優先級。下表是「現階段對 ESMO 真正重要」的前 5 名——依**槓桿**（擋住最多後續工作／每天都在付的代價）排序，而非傳統嚴重度。完整 TD-01～15 仍保留在下方第二節。

| 名次 | TD | 為什麼現在最重要 |
|------|----|------------------|
| ~~1~~ | ~~TD-04~~ | ✅ **已解決（2026-06-30，R0.2-03）**：已建 `src/main.jsx` 掛載主線 GameView，靜態驗證主線連通且 legacy 隔離。 |
| 1（現） | **TD-05** | 你的明確目標就是交接給其他 AI／GitHub，而 README 指向的 onboarding 文件目前是空殼。純文件、零風險、直接服務交接。**現為實質第一名。** |
| 2 | **TD-12** | `LogicEngine` 是 B 主線的引擎核心。為它補單元測試＝替未來一年的數值改動裝上安全網，投報率最高。 |
| 3 | **TD-10** | `frustumCulled` 是反覆回歸的 bug，且發生在主線渲染路徑（`MobaView3D`）上。範圍小、能根除。 |
| 4 | **TD-11** | `vite_config.js` 檔名底線使 Vite 不載入 react plugin——這是「實際 `npm run dev` 能不能起來」的硬阻擋（R0.2-04）。一行改名即解。 |
| 5 | **TD-06** | base64 抽離，但**範圍縮小**為「從 legacy A 移植功能進 B 時順帶處理」。它住在 legacy 內，已不是每日成本，只在移植時才需面對。 |

**決策一次解決／降級了多少債（不寫一行程式）：**
- **TD-08（產品定位／主線未定）→ 已解決**：主線拍板即此項。
- **TD-01（版本血脈衝突）→ 大幅消解**：`package.json` 的 r160 對主線 B 本來就是對的；A 的 r128 舊 API 因 A 不再被建置而**無需修**。殘留風險僅「確認 B 沒有意外相依 A」。
- **TD-02／TD-03（重名函式）、TD-09（monolith 過大）→ 降為 Legacy 關注**：都在不再維護的 A 內，不再是主線的當務之急。
- **TD-07（平行副本）→ 主線部分已解決**：「哪份是真的」已由決策回答。

> 這也回頭印證了「先做決策、再動程式」是對的：一個零程式碼的決策，消解或降級了約五項債。

---

## 一、架構快照

ESMO 存在兩條血脈。**2026-06-30 起，兩者的定位已正式拍板：**

**B. 主線（Trunk）— 唯一長期維護的架構** — React + Vite + React Three Fiber（three r160）。
- `gameData.js` / `LogicEngine.js` / `useGameStore.js` / `useLocalServer.js` / `MobaView3D.jsx` / `GameView.jsx`
- `data/heroes.js`（2026-06-30 新增）：英雄資料單一真實來源。`ROLE_STATS`/`BASE_HP` 已接入 LogicEngine（取代原本寫死的數值陣列，行為零變更）；`CHAMPIONS_100`（100 位，自 Legacy 逐位元抽出）為參考名單、尚未接入模擬。**註：此為 Hero System 資料化的第一步，非關閉任何編號 TD**（TD-06 base64 與此無關）。
- `data/heroMapping.js`（2026-06-30 新增）：Hero Mapping Layer，將 10 名玩家對應到 `CHAMPIONS_100` 一位英雄（身份映射；純函式、零相依）。`roster` 存於 `useGameStore`、開局由 `useLocalServer` 指派。**戰鬥零影響、LogicEngine 未改動；同樣非關閉任何編號 TD**（Hero System 進度）。
- `package.json` 宣告 three `^0.160.0` + R3F 8 + drei 9 + postprocessing + zustand。
- **這是 ESMO 唯一的長期維護主線。** 所有新開發都在此進行。

**A. Legacy Prototype — 只供參考與功能移植，不再維護** — 為 Claude artifact 沙盒撰寫，使用 three.js r128 舊 API。
- `EsportsGame.jsx`（9,401 行）：功能完整的沙盒成品。含 MOBA／FPS 3D 觀戰、約 22 個經營子系統、`CHAMPIONS_100`/`SKILL_DB`/`PERSONALITY` 等資料與內聯 base64 資產。
- `App.jsx`（803 行）、`EsportsFPS3D.jsx`（1,584 行）：同屬沙盒血脈。
- **定位：功能與資料的「來源庫」。** 後續工作是把這裡的功能逐步**移植**進 B，而非繼續在 A 上開發。A 的 r128 舊 API（`sRGBEncoding` 等）**無需修復**，因為 A 不再被建置為產品。

### 建置進入點
`index.html` → `/src/main.jsx`。**`main.jsx` 不在目前的專案檔內**。依主線決策，它**必須掛載 B 主線（`<GameView/>`）**，不是 legacy 的 `<EsportsGame/>`。見 TD-04 / R0.2-03。

### snapshot 合約（B 血脈的核心，render 只依賴它）
詳見 `INTEGRATION.md`。重點：`LogicEngine.snapshot()` 維持形狀不變時，render 層與 store 一行都不用改。這是這個專案最值得保護的設計。

---

## 二、技術債務清單

嚴重度：🔴 Critical｜🟠 High｜🟡 Medium｜⚪ Low

| ID | 嚴重度 | 標題 | 位置 | 說明 | 狀態 |
|----|--------|------|------|------|------|
| TD-01 | 🔴 | three.js 版本血脈衝突 | `package.json`（three ^0.160）vs `EsportsGame.jsx` L667/690/696/717/884、`App.jsx` L113/397/435/464、`EsportsFPS3D.jsx` L546/569/575/596/763 | 宣告 r160，但成品程式用 r128 已移除的 `sRGBEncoding`/`.encoding`/`outputEncoding`。主線已定為 B（r160），`package.json` 本來就對；A 的 r128 舊 API 因 A 為 legacy 不再建置而無需修。殘留：確認 B 未意外相依 A。 | 大幅消解（2026-06-30 決策）／僅餘確認 |
| TD-02 | 🔴 | 被覆蓋的頂層函式 `simulateFps` | `EsportsGame.jsx` L351、L3628 | 兩個同名頂層 `function`，後者覆蓋前者，前者形同死碼或「本該被呼叫卻沒被呼叫」的版本。需確認現行版並移除/更名另一個。 | 待處理 |
| TD-03 | 🔴 | 被覆蓋的頂層元件 `HeroAvatar` | `EsportsGame.jsx` L6357、L8086 | 兩個同名頂層元件、簽名不同，後者全域覆蓋前者，可能造成靜默的渲染錯誤。 | 待處理 |
| TD-04 | 🔴 | 建置進入點缺失/未確認 | `index.html` → `/src/main.jsx` | 已新建 `src/main.jsx` 掛載主線 `<GameView/>`（不經 App.jsx）。離線靜態驗證：本地 import 全解析、LogicEngine 在鏈上、未走到 App.jsx、外部相依齊全。 | ✅ 已解決（2026-06-30，R0.2-03） |
| TD-05 | 🟠 | 文件骨架空殼 | `02_遊戲設計_GDD_.md`～`07_待辦事項.md` 皆 0 bytes | GDD／路線圖／更新日誌／英雄庫／技能庫／待辦全空，但 README 與 `00_AI專案說明.md` 將其列為 AI onboarding 路徑。 | 待處理 |
| TD-06 | 🟠 | 內聯 base64 資產 | `EsportsGame.jsx` L1769（MOBA_MAP）、L1771（HERO_IMG）、L2150（CS_MAPS）等；`App.jsx` L22 | 約 700KB base64 直接寫死在原始碼，使單檔達 1.5MB，每次編輯與 AI 讀檔成本極高。 | 待處理 |
| TD-07 | 🟠 | 三套平行 MOBA／兩套平行 FPS | A 血脈 vs B 血脈；FpsModule（內聯）vs `EsportsFPS3D.jsx` | 同一邏輯有多份副本。「哪份是真的」已由主線決策回答（B 為準，A 為 legacy 來源庫）。殘留：移植時逐一比對。 | 主線部分已解決（2026-06-30）｜降級 |
| TD-08 | 🟠 | 產品定位／主線未定 | README vs 實作 | 主線血脈未定，影響版本對齊、副本收斂、LogicEngine 深化等多項決策。 | ✅ 已解決（2026-06-30）：B＝唯一主線，A＝Legacy Prototype |
| TD-09 | 🟡 | monolith 單檔過大 | `EsportsGame.jsx` 9,401 行 | 約 24 個 Module IIFE + 160 個函式塞在一檔，需靠 grep 導航，超出多數 AI 單次上下文。 | 待處理 |
| TD-10 | 🟡 | `frustumCulled` 覆蓋面待驗證 | `EsportsGame.jsx`（全檔僅 1 處） | 已知「玩家 mesh 必須 `frustumCulled=false`」是反覆性修正；需確認所有玩家 mesh 建立點都覆蓋到。 | 待處理 |
| TD-11 | 🟡 | Vite 設定檔命名 | `vite_config.js` | Vite 預設載入 `vite.config.js`（點，非底線）。若非上傳改名造成，正式專案不會自動載入此設定。 | 待處理 |
| TD-12 | 🟡 | `LogicEngine` 無測試 | `LogicEngine.js` | 純邏輯、零相依，最適合單元測試，目前完全沒有；任何數值改動只能靠人工驗證。 | 待處理 |
| TD-13 | ⚪ | 命令式 `useFrame` 非宣告式 R3F | `MobaView3D.jsx` | 針對大量物件的效能取捨，可接受，**不建議**為純粹性重寫。 | 接受現狀 |
| TD-14 | ⚪ | FPS 以 IIFE 內聯 | `EsportsGame.jsx`（FpsModule） | 沙盒無法 import 的合理妥協，**不建議**現在處理。 | 接受現狀 |
| TD-15 | ⚪ | 各 IIFE 內重複小工具 | `EsportsGame.jsx`（lerp/clamp/dist/ease/mkRng 等多份） | IIFE 作用域內不衝突，純重複、無害。 | 接受現狀 |

---

## 三、已知的反覆性陷阱（接手前必讀）

這些不是待修債務，而是「改某類東西時必須記得做的事」，記錄於此避免重蹈覆轍：

- **Frustum culling**：玩家 mesh 一律 `frustumCulled=false`，否則會閃爍/消失。
- **`MAP_EDGE` 平衡敏感**：任何戰鬥技能模型改動後，需重調 map edge 以回到約 50% T 方勝率。
- **箱子碰撞**：完整箱子碰撞會破壞平衡並卡住角色；只做車輛碰撞才是穩定模式。
- **相機抖動**：`camBlocked` 遮擋迴避會在密集地圖震盪；改用建築淡出透明，不要用相機旋轉迴避。
- **邏輯/渲染純度**：`LogicEngine.js` 不可 import React/Zustand/DOM，否則無法搬 Node.js。
- **內插邊界**：內插是 client 的事；store 存 prev+latest，view 以 60fps lerp，logic 跑 6Hz。

---

## 四、使用方式（給 GitHub／AI）

1. 開 issue 時以 ID 開頭，方便雙向追蹤。
2. 解決後在本表「狀態」欄填 `已解決 #PR編號`，並同步更新 `04_更新日誌.md`。
3. 對應的執行計畫見 `03_開發路線圖.md`（v0.2 任務會引用此處 ID）。
